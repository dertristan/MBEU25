{
  "hash": "b4f4795b66b510055f062f36459c9c84",
  "result": {
    "engine": "knitr",
    "markdown": "---\ntitle: \"GRF Post-Processing\"\nsubtitle: |\n  GRF Post-Processing — ATE, Heterogeneity Diagnostics, and CATE by Moderator (Dictator + Trust)\ndate: last-modified\ndate-format: MMMM D, YYYY\nformat:\n  html:\n    toc: true\n    toc-depth: 3\n    code-fold: true\n    code-tools: true\nexecute:\n  echo: true\n  warning: true\n  eval: true\n  message: true\n---\n\n## Purpose {#sec-purpose}\n\nThis notebook post-processes the **primary** estimator — the saved `grf` causal forests\n(`data/03_final/grf_{dictator,trust}.rds`) — following the canonical post-fit workflow from\nthe generalized-random-forest literature (`athey2019generalized`, `wager2018estimation`,\n`athey2019estimating`).\n\nIt is written to be read **line by line**: every step is laid out sequentially, the two\ngames (Dictator, Trust) are done one after the other, and the headline CATE-by-moderator\nfigures are built **one explicit chunk per moderator** rather than through shared helper\nfunctions.\n\nFor each game we run, in order:\n\n1. **Average treatment effect (ATE) + SE** — `average_treatment_effect()`, the doubly-robust\n   (AIPW) estimate with the cluster-robust standard error from the respondent clusters stored\n   on the forest.\n2. **Targeting Operator Characteristic (TOC) / RATE** — `rank_average_treatment_effect()`,\n   the overall \"is there targetable heterogeneity\" summary (AUTOC ± SE plus the TOC curve).\n3. **Best linear projection (BLP)** — `best_linear_projection()` of the CATE onto the\n   moderator set, an appendix-style linear summary of effect modification.\n4. **Variable importance** — `variable_importance()`, aggregated from the expanded dummy\n   columns back up to the original moderators (construct level).\n5. **CATE by moderator** — the headline figures: estimated CATE on the y-axis, the moderator\n   on the x-axis, with an **honest 95% confidence interval at each level**. Per-level\n   intervals come straight from `average_treatment_effect(cf, subset = ...)`, which is the\n   documented `grf` way to get a doubly-robust, cluster-robust subset ATE.\n\nTwo moderator families, matching the research plan:\n\n- **Conjoint round-level** moderators (`prof_mods`) — other attributes in the same profile.\n- **Individual-level** moderators (`resp_mods`) — respondent pre-treatment items and indices.\n\nThe companion notebook `08_postprocess_bcf.qmd` runs the analogous workflow on the BCF fits.\n\n::: callout-note\n## A note on cost\n\nEach per-level CI is a separate `average_treatment_effect()` call, so the CATE-by-moderator\nsweep makes many calls (one per level, across 31 moderators, twice). This is slower than\ncomputing the doubly-robust scores once and reusing them, but it is the most transparent\nroute and the user-requested one; Quarto `freeze: auto` caches the rendered result.\n:::\n\n## Setup {#sec-setup}\n\n\n::: {.cell .hidden}\n\n```{.r .cell-code .hidden}\n#| label: setup\n#| include: false\n# execution time\nstart_time <- Sys.time()\n# console width\noptions(width = 80)\n# packages\np_required <- c(\n  \"tidyverse\", # dplyr, ggplot2, tidyr, purrr\n  \"here\", # relative paths\n  \"ggpubr\", # theme_pubr\n  \"patchwork\", # later composition\n  \"grf\", # causal_forest predictions, ATE, RATE, BLP\n  \"sessioninfo\" # session docs\n)\npackages <- rownames(installed.packages())\np_to_install <- p_required[!(p_required %in% packages)]\nif (length(p_to_install) > 0) {\n  install.packages(p_to_install)\n}\nsapply(p_required, require, character.only = TRUE)\n```\n\n::: {.cell-output .cell-output-stderr .hidden}\n\n```\nLoading required package: tidyverse\n```\n\n\n:::\n\n::: {.cell-output .cell-output-stderr .hidden}\n\n```\n── Attaching core tidyverse packages ──────────────────────── tidyverse 2.0.0 ──\n✔ dplyr     1.1.4     ✔ readr     2.1.6\n✔ forcats   1.0.1     ✔ stringr   1.6.0\n✔ ggplot2   4.0.1     ✔ tibble    3.3.1\n✔ lubridate 1.9.5     ✔ tidyr     1.3.2\n✔ purrr     1.2.1     \n── Conflicts ────────────────────────────────────────── tidyverse_conflicts() ──\n✖ dplyr::filter() masks stats::filter()\n✖ dplyr::lag()    masks stats::lag()\nℹ Use the conflicted package (<http://conflicted.r-lib.org/>) to force all conflicts to become errors\nLoading required package: here\n\nhere() starts at C:/R/research/MBEU25\n\nLoading required package: ggpubr\n\nLoading required package: patchwork\n\nLoading required package: grf\n\nLoading required package: sessioninfo\n```\n\n\n:::\n\n::: {.cell-output .cell-output-stdout}\n\n```\n  tidyverse        here      ggpubr   patchwork         grf sessioninfo \n       TRUE        TRUE        TRUE        TRUE        TRUE        TRUE \n```\n\n\n:::\n\n```{.r .cell-code .hidden}\n#| label: setup\n#| include: false\nrm(p_required, p_to_install, packages)\n```\n:::\n\n\n## Load data and fits {#sec-load}\n\n\n::: {.cell}\n\n```{.r .cell-code .hidden}\n#| label: load-data\neu25_long <- readRDS(here::here(\"data\", \"02_processed\", \"eu25_long.rds\"))\ncf_dict <- readRDS(here::here(\"data\", \"03_final\", \"grf_dictator.rds\"))\ncf_trust <- readRDS(here::here(\"data\", \"03_final\", \"grf_trust.rds\"))\n```\n:::\n\n\n## Reconstruct the moderator designs {#sec-designs}\n\nThe frozen moderator list, copied verbatim from `06_grf_fit.qmd`.\n\n\n::: {.cell}\n\n```{.r .cell-code .hidden}\n#| label: moderators\nprof_mods <- c(\n  \"cj_nat\",\n  \"der_cj_eu_identity\",\n  \"der_cj_partisanship\",\n  \"cj_age\",\n  \"cj_sex\",\n  \"cj_class\"\n)\nresp_mods <- c(\n  \"q_gender\",\n  \"q_age\",\n  \"q_identity_country\",\n  \"q_identity_eu\",\n  \"q_identity_europe\",\n  \"q_religion\",\n  \"q_class\",\n  \"q_eu_efficacy_understand\",\n  \"q_pop_reps\",\n  \"q_pop_goodevil\",\n  \"q_pop_compromise\",\n  \"q_dem_compromise\",\n  \"q_dem_listen\",\n  \"q_tech_experts\",\n  \"q_tech_leaders\",\n  \"q_party_harm\",\n  \"q_people_incompetent\",\n  \"q_eu_longterm\",\n  \"q_eu_responsive\",\n  \"q_eu_imp_nat_econ\",\n  \"q_eu_imp_nat_cul\",\n  \"q_eu_imp_nat_pol\",\n  \"q_eu_abolish\",\n  \"q_eu_satisfaction\",\n  \"q_rural_urban\",\n  \"q_edu_age_stop\"\n)\nall_mods <- c(prof_mods, resp_mods)\n```\n:::\n\n\nFor each game we filter `dat` to that game in its original order (the same order the forest\nwas fit on), keep the original moderator columns for plotting (`m_dict` / `m_trust`), and\nbuild the numeric moderator matrix used by the projection and importance steps (`X_dict` /\n`X_trust`) exactly as at fit time.\nThe `stopifnot()` confirms the reconstructed rows line up with the forest's training rows, so\neach τ̂ aligns with its moderator value.\n\n\n::: {.cell}\n\n```{.r .cell-code .hidden}\n#| label: designs\n# dictator design (same row order as the fit)\nsub_dict <- eu25_long |> filter(cj_game_type == \"cj_dict\")\nm_dict <- sub_dict |>\n  select(all_of(all_mods)) |>\n  mutate(across(where(is.character), factor))\nX_dict <- model.matrix(~ . - 1, data = m_dict)\n\n# trust design (same row order as the fit)\nsub_trust <- eu25_long |> filter(cj_game_type == \"cj_trust\")\nm_trust <- sub_trust |>\n  select(all_of(all_mods)) |>\n  mutate(across(where(is.character), factor))\nX_trust <- model.matrix(~ . - 1, data = m_trust)\n\nstopifnot(\n  length(cf_dict$Y.orig) == nrow(m_dict),\n  length(cf_trust$Y.orig) == nrow(m_trust),\n  !anyNA(X_dict),\n  !anyNA(X_trust)\n)\n```\n:::\n\n\nThe y-axis label, shared by every CATE figure.\n\n\n::: {.cell}\n\n```{.r .cell-code .hidden}\n#| label: ylab\nyl <- expression(widehat(CATE))\n```\n:::\n\n\n## Dictator game {#sec-dict}\n\n## Average treatment effect {#sec-dict-ate}\n\nThe doubly-robust ATE with its cluster-robust SE (respondent clusters are stored on the\nforest), and the implied 95% interval.\n\n\n::: {.cell}\n\n```{.r .cell-code .hidden}\n#| label: ate-dict\nate_dict <- grf::average_treatment_effect(cf_dict, target.sample = \"all\")\ncat(sprintf(\n  \"Dictator ATE: %.4f  (SE %.4f);  95%% CI [%.4f, %.4f]\\n\",\n  ate_dict[\"estimate\"],\n  ate_dict[\"std.err\"],\n  ate_dict[\"estimate\"] - 1.96 * ate_dict[\"std.err\"],\n  ate_dict[\"estimate\"] + 1.96 * ate_dict[\"std.err\"]\n))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nDictator ATE: -0.5656  (SE 0.0190);  95% CI [-0.6028, -0.5284]\n```\n\n\n:::\n:::\n\n\n## Targeting Operator Characteristic (RATE) {#sec-dict-toc}\n\n`rank_average_treatment_effect()` ranks observations by their out-of-bag predicted CATE and\ntraces the ATE among the top-`q` fraction against the overall ATE.\nThe area under this curve is the AUTOC; a curve and AUTOC clearly above zero is evidence of\ntargetable heterogeneity.\nPriorities here are the forest's own out-of-bag predictions, which is acceptable because they\nare honest (out-of-bag); a separate train/evaluate split would be the stricter alternative\nbut is out of scope.\n\n\n::: {#cell-fig-grf-dict-toc .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-dict-toc\n#| fig-cap: \"GRF Targeting Operator Characteristic curve (Dictator game): ATE among the top-q fraction by predicted CATE minus the overall ATE, with 95% CI. AUTOC summarises the area under the curve.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nprio_dict <- predict(cf_dict)$predictions\nrate_dict <- grf::rank_average_treatment_effect(cf_dict, prio_dict)\nprint(rate_dict)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n estimate    std.err             target\n  0.27015 0.01890785 priorities | AUTOC\n```\n\n\n:::\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-dict-toc\n#| fig-cap: \"GRF Targeting Operator Characteristic curve (Dictator game): ATE among the top-q fraction by predicted CATE minus the overall ATE, with 95% CI. AUTOC summarises the area under the curve.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\ntoc_dict <- rate_dict$TOC |>\n  mutate(lower = estimate - 1.96 * std.err, upper = estimate + 1.96 * std.err)\n\nggplot(toc_dict, aes(x = q, y = estimate)) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = \"#0072B2\") +\n  geom_line(colour = \"#0072B2\", linewidth = 0.8) +\n  labs(\n    title = sprintf(\n      \"TOC (Dictator): AUTOC = %.3f (SE %.3f)\",\n      rate_dict$estimate,\n      rate_dict$std.err\n    ),\n    x = \"Treated fraction q (highest predicted CATE first)\",\n    y = \"ATE(top q) - ATE(all)\"\n  ) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF Targeting Operator Characteristic curve (Dictator game): ATE among the top-q fraction by predicted CATE minus the overall ATE, with 95% CI. AUTOC summarises the area under the curve.](07_postprocess_grf_files/figure-html/fig-grf-dict-toc-1.png){#fig-grf-dict-toc width=3250}\n:::\n:::\n\n\n## Best linear projection {#sec-dict-blp}\n\nThe best linear projection of the CATE onto the moderator matrix: a linear summary of which\nmoderators shift the treatment effect.\nThis is an appendix/robustness summary, not a headline.\n\n\n::: {#cell-fig-grf-dict-blp .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-dict-blp\n#| fig-cap: \"GRF best linear projection of the CATE onto the moderator set (Dictator game), with 95% CIs. Appendix-style linear summary of effect modification.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 9\nblp_dict <- grf::best_linear_projection(cf_dict, X_dict)\nblp_dict_df <- blp_dict |>\n  unclass() |>\n  as.data.frame() |>\n  tibble::rownames_to_column(\"term\") |>\n  as_tibble() |>\n  rename(estimate = Estimate, std_error = `Std. Error`) |>\n  mutate(\n    lower = estimate - 1.96 * std_error,\n    upper = estimate + 1.96 * std_error\n  ) |>\n  filter(term != \"(Intercept)\")\n\nggplot(blp_dict_df, aes(x = estimate, y = fct_reorder(term, estimate))) +\n  geom_vline(xintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_pointrange(aes(xmin = lower, xmax = upper), colour = \"#0072B2\") +\n  labs(\n    title = \"Best linear projection of the CATE (Dictator)\",\n    x = \"Projection coefficient\",\n    y = NULL\n  ) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF best linear projection of the CATE onto the moderator set (Dictator game), with 95% CIs. Appendix-style linear summary of effect modification.](07_postprocess_grf_files/figure-html/fig-grf-dict-blp-1.png){#fig-grf-dict-blp width=3250}\n:::\n:::\n\n\n## Variable importance {#sec-dict-vi}\n\n`variable_importance()` returns one value per design column (the expanded moderator dummies\nplus the country fixed-effect dummies).\nWe map each column back to its source moderator by longest-prefix match and sum, giving a\nconstruct-level ranking; country fixed-effect columns match no moderator and are dropped.\n\n\n::: {#cell-fig-grf-dict-vi .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-dict-vi\n#| fig-cap: \"GRF variable importance (Dictator game), aggregated to the original moderators. Profile-level and respondent-level moderators distinguished by colour. Country fixed-effect columns excluded.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 7\nvi_dict <- grf::variable_importance(cf_dict)\nvi_dict_df <- tibble(\n  column = colnames(cf_dict$X.orig),\n  importance = as.numeric(vi_dict)\n) |>\n  mutate(\n    moderator = map_chr(column, function(cn) {\n      hit <- all_mods[startsWith(cn, all_mods)]\n      if (length(hit) == 0) NA_character_ else hit[which.max(nchar(hit))]\n    })\n  ) |>\n  filter(!is.na(moderator)) |>\n  summarise(importance = sum(importance), .by = moderator) |>\n  mutate(\n    family = if_else(\n      moderator %in% prof_mods,\n      \"Profile-level\",\n      \"Respondent-level\"\n    ),\n    moderator = fct_reorder(moderator, importance)\n  )\n\nggplot(vi_dict_df, aes(x = importance, y = moderator, fill = family)) +\n  geom_col() +\n  scale_fill_manual(\n    values = c(\"Profile-level\" = \"#D55E00\", \"Respondent-level\" = \"#0072B2\")\n  ) +\n  labs(\n    title = \"Variable importance (Dictator)\",\n    x = \"Importance (depth-weighted split frequency)\",\n    y = NULL,\n    fill = NULL\n  ) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF variable importance (Dictator game), aggregated to the original moderators. Profile-level and respondent-level moderators distinguished by colour. Country fixed-effect columns excluded.](07_postprocess_grf_files/figure-html/fig-grf-dict-vi-1.png){#fig-grf-dict-vi width=3250}\n:::\n:::\n\n\n## CATE by moderator — conjoint round-level {#sec-dict-prof}\n\nFor each discrete moderator we loop over its levels, compute the subset ATE with its honest\ncluster-robust SE, form the 95% interval as estimate ± 1.96·SE, and plot the per-level\nestimate (blue) over grey per-level count bars.\n\n### cj_nat {#sec-dict-cj-nat}\n\n\n::: {#cell-fig-grf-dict-cj-nat .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-dict-cj-nat\n#| fig-cap: \"GRF CATE of the Muslim profile by cj_nat (Dictator): per-level subset ATE with honest cluster-robust 95% CI (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"cj_nat\"\nlevs <- sort(unique(m_dict[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_dict[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_dict, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    level = as.character(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(\n    level = factor(level, levels = as.character(levs)),\n    lower = estimate - 1.96 * se,\n    upper = estimate + 1.96 * se\n  )\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = level)) +\n  geom_segment(\n    aes(xend = level, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_linerange(aes(ymin = lower, ymax = upper), colour = \"#0072B2\") +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr() +\n  theme(axis.text.x = element_text(angle = 30, hjust = 1))\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by cj_nat (Dictator): per-level subset ATE with honest cluster-robust 95% CI (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-dict-cj-nat-1.png){#fig-grf-dict-cj-nat width=3250}\n:::\n:::\n\n\n### der_cj_eu_identity {#sec-dict-der-cj-eu-identity}\n\n\n::: {#cell-fig-grf-dict-der-cj-eu-identity .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-dict-der-cj-eu-identity\n#| fig-cap: \"GRF CATE of the Muslim profile by der_cj_eu_identity (Dictator): per-level subset ATE with honest cluster-robust 95% CI (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"der_cj_eu_identity\"\nlevs <- sort(unique(m_dict[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_dict[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_dict, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    level = as.character(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(\n    level = factor(level, levels = as.character(levs)),\n    lower = estimate - 1.96 * se,\n    upper = estimate + 1.96 * se\n  )\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = level)) +\n  geom_segment(\n    aes(xend = level, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_linerange(aes(ymin = lower, ymax = upper), colour = \"#0072B2\") +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr() +\n  theme(axis.text.x = element_text(angle = 30, hjust = 1))\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by der_cj_eu_identity (Dictator): per-level subset ATE with honest cluster-robust 95% CI (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-dict-der-cj-eu-identity-1.png){#fig-grf-dict-der-cj-eu-identity width=3250}\n:::\n:::\n\n\n### der_cj_partisanship {#sec-dict-der-cj-partisanship}\n\n\n::: {#cell-fig-grf-dict-der-cj-partisanship .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-dict-der-cj-partisanship\n#| fig-cap: \"GRF CATE of the Muslim profile by der_cj_partisanship (Dictator): per-level subset ATE with honest cluster-robust 95% CI (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"der_cj_partisanship\"\nlevs <- sort(unique(m_dict[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_dict[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_dict, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    level = as.character(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(\n    level = factor(level, levels = as.character(levs)),\n    lower = estimate - 1.96 * se,\n    upper = estimate + 1.96 * se\n  )\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = level)) +\n  geom_segment(\n    aes(xend = level, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_linerange(aes(ymin = lower, ymax = upper), colour = \"#0072B2\") +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr() +\n  theme(axis.text.x = element_text(angle = 30, hjust = 1))\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by der_cj_partisanship (Dictator): per-level subset ATE with honest cluster-robust 95% CI (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-dict-der-cj-partisanship-1.png){#fig-grf-dict-der-cj-partisanship width=3250}\n:::\n:::\n\n\n### cj_age {#sec-dict-cj-age}\n\n\n::: {#cell-fig-grf-dict-cj-age .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-dict-cj-age\n#| fig-cap: \"GRF CATE of the Muslim profile by cj_age (Dictator): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"cj_age\"\nlevs <- sort(unique(m_dict[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_dict[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_dict, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    value = as.numeric(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(lower = estimate - 1.96 * se, upper = estimate + 1.96 * se)\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = value)) +\n  geom_segment(\n    aes(xend = value, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = \"#0072B2\") +\n  geom_line(aes(y = estimate), colour = \"#0072B2\", linewidth = 0.8) +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by cj_age (Dictator): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-dict-cj-age-1.png){#fig-grf-dict-cj-age width=3250}\n:::\n:::\n\n\n### cj_sex {#sec-dict-cj-sex}\n\n\n::: {#cell-fig-grf-dict-cj-sex .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-dict-cj-sex\n#| fig-cap: \"GRF CATE of the Muslim profile by cj_sex (Dictator): per-level subset ATE with honest cluster-robust 95% CI (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"cj_sex\"\nlevs <- sort(unique(m_dict[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_dict[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_dict, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    level = as.character(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(\n    level = factor(level, levels = as.character(levs)),\n    lower = estimate - 1.96 * se,\n    upper = estimate + 1.96 * se\n  )\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = level)) +\n  geom_segment(\n    aes(xend = level, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_linerange(aes(ymin = lower, ymax = upper), colour = \"#0072B2\") +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr() +\n  theme(axis.text.x = element_text(angle = 30, hjust = 1))\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by cj_sex (Dictator): per-level subset ATE with honest cluster-robust 95% CI (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-dict-cj-sex-1.png){#fig-grf-dict-cj-sex width=3250}\n:::\n:::\n\n\n### cj_class {#sec-dict-cj-class}\n\n\n::: {#cell-fig-grf-dict-cj-class .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-dict-cj-class\n#| fig-cap: \"GRF CATE of the Muslim profile by cj_class (Dictator): per-level subset ATE with honest cluster-robust 95% CI (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"cj_class\"\nlevs <- sort(unique(m_dict[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_dict[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_dict, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    level = as.character(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(\n    level = factor(level, levels = as.character(levs)),\n    lower = estimate - 1.96 * se,\n    upper = estimate + 1.96 * se\n  )\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = level)) +\n  geom_segment(\n    aes(xend = level, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_linerange(aes(ymin = lower, ymax = upper), colour = \"#0072B2\") +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr() +\n  theme(axis.text.x = element_text(angle = 30, hjust = 1))\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by cj_class (Dictator): per-level subset ATE with honest cluster-robust 95% CI (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-dict-cj-class-1.png){#fig-grf-dict-cj-class width=3250}\n:::\n:::\n\n\n## CATE by moderator — respondent-level {#sec-dict-resp}\n\nSame per-level construction for the respondent moderators.\nThe two genuinely continuous moderators — `q_age` and `q_edu_age_stop` — are instead split\ninto ten quantile bins and drawn as a ribbon-and-line with a covariate rug.\n\n### q_gender {#sec-dict-q-gender}\n\n\n::: {#cell-fig-grf-dict-q-gender .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-dict-q-gender\n#| fig-cap: \"GRF CATE of the Muslim profile by q_gender (Dictator): per-level subset ATE with honest cluster-robust 95% CI (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_gender\"\nlevs <- sort(unique(m_dict[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_dict[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_dict, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    level = as.character(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(\n    level = factor(level, levels = as.character(levs)),\n    lower = estimate - 1.96 * se,\n    upper = estimate + 1.96 * se\n  )\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = level)) +\n  geom_segment(\n    aes(xend = level, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_linerange(aes(ymin = lower, ymax = upper), colour = \"#0072B2\") +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr() +\n  theme(axis.text.x = element_text(angle = 30, hjust = 1))\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by q_gender (Dictator): per-level subset ATE with honest cluster-robust 95% CI (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-dict-q-gender-1.png){#fig-grf-dict-q-gender width=3250}\n:::\n:::\n\n\n### q_age {#sec-dict-q-age}\n\n\n::: {#cell-fig-grf-dict-q-age .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-dict-q-age\n#| fig-cap: \"GRF CATE of the Muslim profile across deciles of q_age (Dictator): per-bin subset ATE with honest cluster-robust 95% CI (ribbon); grey histogram shows the covariate distribution.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_age\"\ncol <- m_dict[[mod]]\nbin <- ntile(col, 10)\nrows <- list()\nfor (k in sort(unique(bin))) {\n  sel <- bin == k\n  a <- grf::average_treatment_effect(cf_dict, subset = sel)\n  rows[[as.character(k)]] <- tibble(\n    bin = k,\n    x_pos = mean(col[sel]),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(lower = estimate - 1.96 * se, upper = estimate + 1.96 * se)\n# distribution band\nyr <- range(c(cate$lower, cate$upper))\npad <- 0.12 * diff(yr)\nfloor_y <- yr[1] - pad\nspan <- 0.20 * diff(yr)\nh <- hist(col[!is.na(col)], breaks = 30, plot = FALSE)\nhd <- tibble(mid = h$mids, w = diff(h$breaks)[1], cnt = h$counts) |>\n  mutate(bar_top = floor_y + (cnt / max(cnt)) * span)\nggplot(cate, aes(x = x_pos)) +\n  geom_rect(\n    data = hd,\n    aes(xmin = mid - w / 2, xmax = mid + w / 2, ymin = floor_y, ymax = bar_top),\n    fill = \"grey80\",\n    inherit.aes = FALSE\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = \"#0072B2\") +\n  geom_line(aes(y = estimate), colour = \"#0072B2\", linewidth = 0.8) +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 1.5) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile across deciles of q_age (Dictator): per-bin subset ATE with honest cluster-robust 95% CI (ribbon); grey histogram shows the covariate distribution.](07_postprocess_grf_files/figure-html/fig-grf-dict-q-age-1.png){#fig-grf-dict-q-age width=3250}\n:::\n:::\n\n\n### q_identity_country {#sec-dict-q-identity-country}\n\n\n::: {#cell-fig-grf-dict-q-identity-country .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-dict-q-identity-country\n#| fig-cap: \"GRF CATE of the Muslim profile by q_identity_country (Dictator): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_identity_country\"\nlevs <- sort(unique(m_dict[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_dict[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_dict, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    value = as.numeric(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(lower = estimate - 1.96 * se, upper = estimate + 1.96 * se)\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = value)) +\n  geom_segment(\n    aes(xend = value, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = \"#0072B2\") +\n  geom_line(aes(y = estimate), colour = \"#0072B2\", linewidth = 0.8) +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by q_identity_country (Dictator): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-dict-q-identity-country-1.png){#fig-grf-dict-q-identity-country width=3250}\n:::\n:::\n\n\n### q_identity_eu {#sec-dict-q-identity-eu}\n\n\n::: {#cell-fig-grf-dict-q-identity-eu .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-dict-q-identity-eu\n#| fig-cap: \"GRF CATE of the Muslim profile by q_identity_eu (Dictator): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_identity_eu\"\nlevs <- sort(unique(m_dict[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_dict[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_dict, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    value = as.numeric(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(lower = estimate - 1.96 * se, upper = estimate + 1.96 * se)\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = value)) +\n  geom_segment(\n    aes(xend = value, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = \"#0072B2\") +\n  geom_line(aes(y = estimate), colour = \"#0072B2\", linewidth = 0.8) +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by q_identity_eu (Dictator): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-dict-q-identity-eu-1.png){#fig-grf-dict-q-identity-eu width=3250}\n:::\n:::\n\n\n### q_identity_europe {#sec-dict-q-identity-europe}\n\n\n::: {#cell-fig-grf-dict-q-identity-europe .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-dict-q-identity-europe\n#| fig-cap: \"GRF CATE of the Muslim profile by q_identity_europe (Dictator): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_identity_europe\"\nlevs <- sort(unique(m_dict[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_dict[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_dict, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    value = as.numeric(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(lower = estimate - 1.96 * se, upper = estimate + 1.96 * se)\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = value)) +\n  geom_segment(\n    aes(xend = value, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = \"#0072B2\") +\n  geom_line(aes(y = estimate), colour = \"#0072B2\", linewidth = 0.8) +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by q_identity_europe (Dictator): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-dict-q-identity-europe-1.png){#fig-grf-dict-q-identity-europe width=3250}\n:::\n:::\n\n\n### q_religion {#sec-dict-q-religion}\n\n\n::: {#cell-fig-grf-dict-q-religion .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-dict-q-religion\n#| fig-cap: \"GRF CATE of the Muslim profile by q_religion (Dictator): per-level subset ATE with honest cluster-robust 95% CI (blue); grey bars show per-level counts. Rare categories give wide CIs.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_religion\"\nlevs <- sort(unique(m_dict[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_dict[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_dict, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    level = as.character(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(\n    level = factor(level, levels = as.character(levs)),\n    lower = estimate - 1.96 * se,\n    upper = estimate + 1.96 * se\n  )\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = level)) +\n  geom_segment(\n    aes(xend = level, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_linerange(aes(ymin = lower, ymax = upper), colour = \"#0072B2\") +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr() +\n  theme(axis.text.x = element_text(angle = 30, hjust = 1))\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by q_religion (Dictator): per-level subset ATE with honest cluster-robust 95% CI (blue); grey bars show per-level counts. Rare categories give wide CIs.](07_postprocess_grf_files/figure-html/fig-grf-dict-q-religion-1.png){#fig-grf-dict-q-religion width=3250}\n:::\n:::\n\n\n### q_class {#sec-dict-q-class}\n\n\n::: {#cell-fig-grf-dict-q-class .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-dict-q-class\n#| fig-cap: \"GRF CATE of the Muslim profile by q_class (Dictator): per-level subset ATE with honest cluster-robust 95% CI (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_class\"\nlevs <- sort(unique(m_dict[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_dict[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_dict, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    level = as.character(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(\n    level = factor(level, levels = as.character(levs)),\n    lower = estimate - 1.96 * se,\n    upper = estimate + 1.96 * se\n  )\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = level)) +\n  geom_segment(\n    aes(xend = level, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_linerange(aes(ymin = lower, ymax = upper), colour = \"#0072B2\") +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr() +\n  theme(axis.text.x = element_text(angle = 30, hjust = 1))\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by q_class (Dictator): per-level subset ATE with honest cluster-robust 95% CI (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-dict-q-class-1.png){#fig-grf-dict-q-class width=3250}\n:::\n:::\n\n\n### q_eu_efficacy_understand {#sec-dict-q-eu-efficacy-understand}\n\n\n::: {#cell-fig-grf-dict-q-eu-efficacy-understand .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-dict-q-eu-efficacy-understand\n#| fig-cap: \"GRF CATE of the Muslim profile by q_eu_efficacy_understand (Dictator): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_eu_efficacy_understand\"\nlevs <- sort(unique(m_dict[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_dict[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_dict, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    value = as.numeric(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(lower = estimate - 1.96 * se, upper = estimate + 1.96 * se)\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = value)) +\n  geom_segment(\n    aes(xend = value, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = \"#0072B2\") +\n  geom_line(aes(y = estimate), colour = \"#0072B2\", linewidth = 0.8) +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by q_eu_efficacy_understand (Dictator): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-dict-q-eu-efficacy-understand-1.png){#fig-grf-dict-q-eu-efficacy-understand width=3250}\n:::\n:::\n\n\n### q_pop_reps {#sec-dict-q-pop-reps}\n\n\n::: {#cell-fig-grf-dict-q-pop-reps .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-dict-q-pop-reps\n#| fig-cap: \"GRF CATE of the Muslim profile by q_pop_reps (Dictator): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_pop_reps\"\nlevs <- sort(unique(m_dict[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_dict[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_dict, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    value = as.numeric(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(lower = estimate - 1.96 * se, upper = estimate + 1.96 * se)\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = value)) +\n  geom_segment(\n    aes(xend = value, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = \"#0072B2\") +\n  geom_line(aes(y = estimate), colour = \"#0072B2\", linewidth = 0.8) +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by q_pop_reps (Dictator): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-dict-q-pop-reps-1.png){#fig-grf-dict-q-pop-reps width=3250}\n:::\n:::\n\n\n### q_pop_goodevil {#sec-dict-q-pop-goodevil}\n\n\n::: {#cell-fig-grf-dict-q-pop-goodevil .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-dict-q-pop-goodevil\n#| fig-cap: \"GRF CATE of the Muslim profile by q_pop_goodevil (Dictator): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_pop_goodevil\"\nlevs <- sort(unique(m_dict[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_dict[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_dict, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    value = as.numeric(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(lower = estimate - 1.96 * se, upper = estimate + 1.96 * se)\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = value)) +\n  geom_segment(\n    aes(xend = value, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = \"#0072B2\") +\n  geom_line(aes(y = estimate), colour = \"#0072B2\", linewidth = 0.8) +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by q_pop_goodevil (Dictator): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-dict-q-pop-goodevil-1.png){#fig-grf-dict-q-pop-goodevil width=3250}\n:::\n:::\n\n\n### q_pop_compromise {#sec-dict-q-pop-compromise}\n\n\n::: {#cell-fig-grf-dict-q-pop-compromise .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-dict-q-pop-compromise\n#| fig-cap: \"GRF CATE of the Muslim profile by q_pop_compromise (Dictator): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_pop_compromise\"\nlevs <- sort(unique(m_dict[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_dict[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_dict, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    value = as.numeric(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(lower = estimate - 1.96 * se, upper = estimate + 1.96 * se)\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = value)) +\n  geom_segment(\n    aes(xend = value, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = \"#0072B2\") +\n  geom_line(aes(y = estimate), colour = \"#0072B2\", linewidth = 0.8) +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by q_pop_compromise (Dictator): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-dict-q-pop-compromise-1.png){#fig-grf-dict-q-pop-compromise width=3250}\n:::\n:::\n\n\n### q_dem_compromise {#sec-dict-q-dem-compromise}\n\n\n::: {#cell-fig-grf-dict-q-dem-compromise .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-dict-q-dem-compromise\n#| fig-cap: \"GRF CATE of the Muslim profile by q_dem_compromise (Dictator): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_dem_compromise\"\nlevs <- sort(unique(m_dict[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_dict[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_dict, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    value = as.numeric(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(lower = estimate - 1.96 * se, upper = estimate + 1.96 * se)\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = value)) +\n  geom_segment(\n    aes(xend = value, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = \"#0072B2\") +\n  geom_line(aes(y = estimate), colour = \"#0072B2\", linewidth = 0.8) +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by q_dem_compromise (Dictator): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-dict-q-dem-compromise-1.png){#fig-grf-dict-q-dem-compromise width=3250}\n:::\n:::\n\n\n### q_dem_listen {#sec-dict-q-dem-listen}\n\n\n::: {#cell-fig-grf-dict-q-dem-listen .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-dict-q-dem-listen\n#| fig-cap: \"GRF CATE of the Muslim profile by q_dem_listen (Dictator): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_dem_listen\"\nlevs <- sort(unique(m_dict[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_dict[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_dict, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    value = as.numeric(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(lower = estimate - 1.96 * se, upper = estimate + 1.96 * se)\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = value)) +\n  geom_segment(\n    aes(xend = value, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = \"#0072B2\") +\n  geom_line(aes(y = estimate), colour = \"#0072B2\", linewidth = 0.8) +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by q_dem_listen (Dictator): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-dict-q-dem-listen-1.png){#fig-grf-dict-q-dem-listen width=3250}\n:::\n:::\n\n\n### q_tech_experts {#sec-dict-q-tech-experts}\n\n\n::: {#cell-fig-grf-dict-q-tech-experts .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-dict-q-tech-experts\n#| fig-cap: \"GRF CATE of the Muslim profile by q_tech_experts (Dictator): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_tech_experts\"\nlevs <- sort(unique(m_dict[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_dict[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_dict, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    value = as.numeric(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(lower = estimate - 1.96 * se, upper = estimate + 1.96 * se)\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = value)) +\n  geom_segment(\n    aes(xend = value, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = \"#0072B2\") +\n  geom_line(aes(y = estimate), colour = \"#0072B2\", linewidth = 0.8) +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by q_tech_experts (Dictator): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-dict-q-tech-experts-1.png){#fig-grf-dict-q-tech-experts width=3250}\n:::\n:::\n\n\n### q_tech_leaders {#sec-dict-q-tech-leaders}\n\n\n::: {#cell-fig-grf-dict-q-tech-leaders .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-dict-q-tech-leaders\n#| fig-cap: \"GRF CATE of the Muslim profile by q_tech_leaders (Dictator): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_tech_leaders\"\nlevs <- sort(unique(m_dict[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_dict[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_dict, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    value = as.numeric(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(lower = estimate - 1.96 * se, upper = estimate + 1.96 * se)\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = value)) +\n  geom_segment(\n    aes(xend = value, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = \"#0072B2\") +\n  geom_line(aes(y = estimate), colour = \"#0072B2\", linewidth = 0.8) +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by q_tech_leaders (Dictator): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-dict-q-tech-leaders-1.png){#fig-grf-dict-q-tech-leaders width=3250}\n:::\n:::\n\n\n### q_party_harm {#sec-dict-q-party-harm}\n\n\n::: {#cell-fig-grf-dict-q-party-harm .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-dict-q-party-harm\n#| fig-cap: \"GRF CATE of the Muslim profile by q_party_harm (Dictator): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_party_harm\"\nlevs <- sort(unique(m_dict[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_dict[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_dict, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    value = as.numeric(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(lower = estimate - 1.96 * se, upper = estimate + 1.96 * se)\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = value)) +\n  geom_segment(\n    aes(xend = value, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = \"#0072B2\") +\n  geom_line(aes(y = estimate), colour = \"#0072B2\", linewidth = 0.8) +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by q_party_harm (Dictator): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-dict-q-party-harm-1.png){#fig-grf-dict-q-party-harm width=3250}\n:::\n:::\n\n\n### q_people_incompetent {#sec-dict-q-people-incompetent}\n\n\n::: {#cell-fig-grf-dict-q-people-incompetent .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-dict-q-people-incompetent\n#| fig-cap: \"GRF CATE of the Muslim profile by q_people_incompetent (Dictator): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_people_incompetent\"\nlevs <- sort(unique(m_dict[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_dict[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_dict, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    value = as.numeric(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(lower = estimate - 1.96 * se, upper = estimate + 1.96 * se)\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = value)) +\n  geom_segment(\n    aes(xend = value, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = \"#0072B2\") +\n  geom_line(aes(y = estimate), colour = \"#0072B2\", linewidth = 0.8) +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by q_people_incompetent (Dictator): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-dict-q-people-incompetent-1.png){#fig-grf-dict-q-people-incompetent width=3250}\n:::\n:::\n\n\n### q_eu_longterm {#sec-dict-q-eu-longterm}\n\n\n::: {#cell-fig-grf-dict-q-eu-longterm .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-dict-q-eu-longterm\n#| fig-cap: \"GRF CATE of the Muslim profile by q_eu_longterm (Dictator): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_eu_longterm\"\nlevs <- sort(unique(m_dict[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_dict[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_dict, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    value = as.numeric(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(lower = estimate - 1.96 * se, upper = estimate + 1.96 * se)\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = value)) +\n  geom_segment(\n    aes(xend = value, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = \"#0072B2\") +\n  geom_line(aes(y = estimate), colour = \"#0072B2\", linewidth = 0.8) +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by q_eu_longterm (Dictator): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-dict-q-eu-longterm-1.png){#fig-grf-dict-q-eu-longterm width=3250}\n:::\n:::\n\n\n### q_eu_responsive {#sec-dict-q-eu-responsive}\n\n\n::: {#cell-fig-grf-dict-q-eu-responsive .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-dict-q-eu-responsive\n#| fig-cap: \"GRF CATE of the Muslim profile by q_eu_responsive (Dictator): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_eu_responsive\"\nlevs <- sort(unique(m_dict[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_dict[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_dict, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    value = as.numeric(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(lower = estimate - 1.96 * se, upper = estimate + 1.96 * se)\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = value)) +\n  geom_segment(\n    aes(xend = value, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = \"#0072B2\") +\n  geom_line(aes(y = estimate), colour = \"#0072B2\", linewidth = 0.8) +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by q_eu_responsive (Dictator): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-dict-q-eu-responsive-1.png){#fig-grf-dict-q-eu-responsive width=3250}\n:::\n:::\n\n\n### q_eu_imp_nat_econ {#sec-dict-q-eu-imp-nat-econ}\n\n\n::: {#cell-fig-grf-dict-q-eu-imp-nat-econ .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-dict-q-eu-imp-nat-econ\n#| fig-cap: \"GRF CATE of the Muslim profile by q_eu_imp_nat_econ (Dictator): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_eu_imp_nat_econ\"\nlevs <- sort(unique(m_dict[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_dict[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_dict, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    value = as.numeric(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(lower = estimate - 1.96 * se, upper = estimate + 1.96 * se)\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = value)) +\n  geom_segment(\n    aes(xend = value, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = \"#0072B2\") +\n  geom_line(aes(y = estimate), colour = \"#0072B2\", linewidth = 0.8) +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by q_eu_imp_nat_econ (Dictator): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-dict-q-eu-imp-nat-econ-1.png){#fig-grf-dict-q-eu-imp-nat-econ width=3250}\n:::\n:::\n\n\n### q_eu_imp_nat_cul {#sec-dict-q-eu-imp-nat-cul}\n\n\n::: {#cell-fig-grf-dict-q-eu-imp-nat-cul .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-dict-q-eu-imp-nat-cul\n#| fig-cap: \"GRF CATE of the Muslim profile by q_eu_imp_nat_cul (Dictator): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_eu_imp_nat_cul\"\nlevs <- sort(unique(m_dict[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_dict[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_dict, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    value = as.numeric(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(lower = estimate - 1.96 * se, upper = estimate + 1.96 * se)\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = value)) +\n  geom_segment(\n    aes(xend = value, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = \"#0072B2\") +\n  geom_line(aes(y = estimate), colour = \"#0072B2\", linewidth = 0.8) +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by q_eu_imp_nat_cul (Dictator): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-dict-q-eu-imp-nat-cul-1.png){#fig-grf-dict-q-eu-imp-nat-cul width=3250}\n:::\n:::\n\n\n### q_eu_imp_nat_pol {#sec-dict-q-eu-imp-nat-pol}\n\n\n::: {#cell-fig-grf-dict-q-eu-imp-nat-pol .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-dict-q-eu-imp-nat-pol\n#| fig-cap: \"GRF CATE of the Muslim profile by q_eu_imp_nat_pol (Dictator): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_eu_imp_nat_pol\"\nlevs <- sort(unique(m_dict[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_dict[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_dict, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    value = as.numeric(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(lower = estimate - 1.96 * se, upper = estimate + 1.96 * se)\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = value)) +\n  geom_segment(\n    aes(xend = value, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = \"#0072B2\") +\n  geom_line(aes(y = estimate), colour = \"#0072B2\", linewidth = 0.8) +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by q_eu_imp_nat_pol (Dictator): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-dict-q-eu-imp-nat-pol-1.png){#fig-grf-dict-q-eu-imp-nat-pol width=3250}\n:::\n:::\n\n\n### q_eu_abolish {#sec-dict-q-eu-abolish}\n\n\n::: {#cell-fig-grf-dict-q-eu-abolish .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-dict-q-eu-abolish\n#| fig-cap: \"GRF CATE of the Muslim profile by q_eu_abolish (Dictator): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_eu_abolish\"\nlevs <- sort(unique(m_dict[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_dict[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_dict, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    value = as.numeric(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(lower = estimate - 1.96 * se, upper = estimate + 1.96 * se)\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = value)) +\n  geom_segment(\n    aes(xend = value, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = \"#0072B2\") +\n  geom_line(aes(y = estimate), colour = \"#0072B2\", linewidth = 0.8) +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by q_eu_abolish (Dictator): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-dict-q-eu-abolish-1.png){#fig-grf-dict-q-eu-abolish width=3250}\n:::\n:::\n\n\n### q_eu_satisfaction {#sec-dict-q-eu-satisfaction}\n\n\n::: {#cell-fig-grf-dict-q-eu-satisfaction .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-dict-q-eu-satisfaction\n#| fig-cap: \"GRF CATE of the Muslim profile by q_eu_satisfaction (Dictator): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_eu_satisfaction\"\nlevs <- sort(unique(m_dict[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_dict[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_dict, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    value = as.numeric(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(lower = estimate - 1.96 * se, upper = estimate + 1.96 * se)\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = value)) +\n  geom_segment(\n    aes(xend = value, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = \"#0072B2\") +\n  geom_line(aes(y = estimate), colour = \"#0072B2\", linewidth = 0.8) +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by q_eu_satisfaction (Dictator): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-dict-q-eu-satisfaction-1.png){#fig-grf-dict-q-eu-satisfaction width=3250}\n:::\n:::\n\n\n### q_rural_urban {#sec-dict-q-rural-urban}\n\n\n::: {#cell-fig-grf-dict-q-rural-urban .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-dict-q-rural-urban\n#| fig-cap: \"GRF CATE of the Muslim profile by q_rural_urban (Dictator): per-level subset ATE with honest cluster-robust 95% CI (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_rural_urban\"\nlevs <- sort(unique(m_dict[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_dict[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_dict, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    level = as.character(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(\n    level = factor(level, levels = as.character(levs)),\n    lower = estimate - 1.96 * se,\n    upper = estimate + 1.96 * se\n  )\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = level)) +\n  geom_segment(\n    aes(xend = level, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_linerange(aes(ymin = lower, ymax = upper), colour = \"#0072B2\") +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr() +\n  theme(axis.text.x = element_text(angle = 30, hjust = 1))\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by q_rural_urban (Dictator): per-level subset ATE with honest cluster-robust 95% CI (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-dict-q-rural-urban-1.png){#fig-grf-dict-q-rural-urban width=3250}\n:::\n:::\n\n\n### q_edu_age_stop {#sec-dict-q-edu-age-stop}\n\n\n::: {#cell-fig-grf-dict-q-edu-age-stop .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-dict-q-edu-age-stop\n#| fig-cap: \"GRF CATE of the Muslim profile across deciles of q_edu_age_stop (Dictator): per-bin subset ATE with honest cluster-robust 95% CI (ribbon); grey histogram shows the covariate distribution.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_edu_age_stop\"\ncol <- m_dict[[mod]]\nbin <- ntile(col, 10)\nrows <- list()\nfor (k in sort(unique(bin))) {\n  sel <- bin == k\n  a <- grf::average_treatment_effect(cf_dict, subset = sel)\n  rows[[as.character(k)]] <- tibble(\n    bin = k,\n    x_pos = mean(col[sel]),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(lower = estimate - 1.96 * se, upper = estimate + 1.96 * se)\n# distribution band\nyr <- range(c(cate$lower, cate$upper))\npad <- 0.12 * diff(yr)\nfloor_y <- yr[1] - pad\nspan <- 0.20 * diff(yr)\nh <- hist(col[!is.na(col)], breaks = 30, plot = FALSE)\nhd <- tibble(mid = h$mids, w = diff(h$breaks)[1], cnt = h$counts) |>\n  mutate(bar_top = floor_y + (cnt / max(cnt)) * span)\nggplot(cate, aes(x = x_pos)) +\n  geom_rect(\n    data = hd,\n    aes(xmin = mid - w / 2, xmax = mid + w / 2, ymin = floor_y, ymax = bar_top),\n    fill = \"grey80\",\n    inherit.aes = FALSE\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = \"#0072B2\") +\n  geom_line(aes(y = estimate), colour = \"#0072B2\", linewidth = 0.8) +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 1.5) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile across deciles of q_edu_age_stop (Dictator): per-bin subset ATE with honest cluster-robust 95% CI (ribbon); grey histogram shows the covariate distribution.](07_postprocess_grf_files/figure-html/fig-grf-dict-q-edu-age-stop-1.png){#fig-grf-dict-q-edu-age-stop width=3250}\n:::\n:::\n\n\n## Trust game {#sec-trust}\n\n## Average treatment effect {#sec-trust-ate}\n\n\n::: {.cell}\n\n```{.r .cell-code .hidden}\n#| label: ate-trust\nate_trust <- grf::average_treatment_effect(cf_trust, target.sample = \"all\")\ncat(sprintf(\n  \"Trust ATE: %.4f  (SE %.4f);  95%% CI [%.4f, %.4f]\\n\",\n  ate_trust[\"estimate\"],\n  ate_trust[\"std.err\"],\n  ate_trust[\"estimate\"] - 1.96 * ate_trust[\"std.err\"],\n  ate_trust[\"estimate\"] + 1.96 * ate_trust[\"std.err\"]\n))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nTrust ATE: -0.6432  (SE 0.0204);  95% CI [-0.6833, -0.6031]\n```\n\n\n:::\n:::\n\n\n## Targeting Operator Characteristic (RATE) {#sec-trust-toc}\n\n\n::: {#cell-fig-grf-trust-toc .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-trust-toc\n#| fig-cap: \"GRF Targeting Operator Characteristic curve (Trust game): ATE among the top-q fraction by predicted CATE minus the overall ATE, with 95% CI. AUTOC summarises the area under the curve.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nprio_trust <- predict(cf_trust)$predictions\nrate_trust <- grf::rank_average_treatment_effect(cf_trust, prio_trust)\nprint(rate_trust)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n  estimate    std.err             target\n 0.2756111 0.01796879 priorities | AUTOC\n```\n\n\n:::\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-trust-toc\n#| fig-cap: \"GRF Targeting Operator Characteristic curve (Trust game): ATE among the top-q fraction by predicted CATE minus the overall ATE, with 95% CI. AUTOC summarises the area under the curve.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\ntoc_trust <- rate_trust$TOC |>\n  mutate(lower = estimate - 1.96 * std.err, upper = estimate + 1.96 * std.err)\n\nggplot(toc_trust, aes(x = q, y = estimate)) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = \"#0072B2\") +\n  geom_line(colour = \"#0072B2\", linewidth = 0.8) +\n  labs(\n    title = sprintf(\n      \"TOC (Trust): AUTOC = %.3f (SE %.3f)\",\n      rate_trust$estimate,\n      rate_trust$std.err\n    ),\n    x = \"Treated fraction q (highest predicted CATE first)\",\n    y = \"ATE(top q) - ATE(all)\"\n  ) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF Targeting Operator Characteristic curve (Trust game): ATE among the top-q fraction by predicted CATE minus the overall ATE, with 95% CI. AUTOC summarises the area under the curve.](07_postprocess_grf_files/figure-html/fig-grf-trust-toc-1.png){#fig-grf-trust-toc width=3250}\n:::\n:::\n\n\n## Best linear projection {#sec-trust-blp}\n\n\n::: {#cell-fig-grf-trust-blp .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-trust-blp\n#| fig-cap: \"GRF best linear projection of the CATE onto the moderator set (Trust game), with 95% CIs. Appendix-style linear summary of effect modification.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 9\nblp_trust <- grf::best_linear_projection(cf_trust, X_trust)\nblp_trust_df <- blp_trust |>\n  unclass() |>\n  as.data.frame() |>\n  tibble::rownames_to_column(\"term\") |>\n  as_tibble() |>\n  rename(estimate = Estimate, std_error = `Std. Error`) |>\n  mutate(\n    lower = estimate - 1.96 * std_error,\n    upper = estimate + 1.96 * std_error\n  ) |>\n  filter(term != \"(Intercept)\")\n\nggplot(blp_trust_df, aes(x = estimate, y = fct_reorder(term, estimate))) +\n  geom_vline(xintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_pointrange(aes(xmin = lower, xmax = upper), colour = \"#0072B2\") +\n  labs(\n    title = \"Best linear projection of the CATE (Trust)\",\n    x = \"Projection coefficient\",\n    y = NULL\n  ) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF best linear projection of the CATE onto the moderator set (Trust game), with 95% CIs. Appendix-style linear summary of effect modification.](07_postprocess_grf_files/figure-html/fig-grf-trust-blp-1.png){#fig-grf-trust-blp width=3250}\n:::\n:::\n\n\n## Variable importance {#sec-trust-vi}\n\n\n::: {#cell-fig-grf-trust-vi .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-trust-vi\n#| fig-cap: \"GRF variable importance (Trust game), aggregated to the original moderators. Profile-level and respondent-level moderators distinguished by colour. Country fixed-effect columns excluded.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 7\nvi_trust <- grf::variable_importance(cf_trust)\nvi_trust_df <- tibble(\n  column = colnames(cf_trust$X.orig),\n  importance = as.numeric(vi_trust)\n) |>\n  mutate(\n    moderator = map_chr(column, function(cn) {\n      hit <- all_mods[startsWith(cn, all_mods)]\n      if (length(hit) == 0) NA_character_ else hit[which.max(nchar(hit))]\n    })\n  ) |>\n  filter(!is.na(moderator)) |>\n  summarise(importance = sum(importance), .by = moderator) |>\n  mutate(\n    family = if_else(\n      moderator %in% prof_mods,\n      \"Profile-level\",\n      \"Respondent-level\"\n    ),\n    moderator = fct_reorder(moderator, importance)\n  )\n\nggplot(vi_trust_df, aes(x = importance, y = moderator, fill = family)) +\n  geom_col() +\n  scale_fill_manual(\n    values = c(\"Profile-level\" = \"#D55E00\", \"Respondent-level\" = \"#0072B2\")\n  ) +\n  labs(\n    title = \"Variable importance (Trust)\",\n    x = \"Importance (depth-weighted split frequency)\",\n    y = NULL,\n    fill = NULL\n  ) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF variable importance (Trust game), aggregated to the original moderators. Profile-level and respondent-level moderators distinguished by colour. Country fixed-effect columns excluded.](07_postprocess_grf_files/figure-html/fig-grf-trust-vi-1.png){#fig-grf-trust-vi width=3250}\n:::\n:::\n\n\n## CATE by moderator — conjoint round-level {#sec-trust-prof}\n\n### cj_nat {#sec-trust-cj-nat}\n\n\n::: {#cell-fig-grf-trust-cj-nat .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-trust-cj-nat\n#| fig-cap: \"GRF CATE of the Muslim profile by cj_nat (Trust): per-level subset ATE with honest cluster-robust 95% CI (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"cj_nat\"\nlevs <- sort(unique(m_trust[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_trust[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_trust, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    level = as.character(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(\n    level = factor(level, levels = as.character(levs)),\n    lower = estimate - 1.96 * se,\n    upper = estimate + 1.96 * se\n  )\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = level)) +\n  geom_segment(\n    aes(xend = level, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_linerange(aes(ymin = lower, ymax = upper), colour = \"#0072B2\") +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr() +\n  theme(axis.text.x = element_text(angle = 30, hjust = 1))\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by cj_nat (Trust): per-level subset ATE with honest cluster-robust 95% CI (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-trust-cj-nat-1.png){#fig-grf-trust-cj-nat width=3250}\n:::\n:::\n\n\n### der_cj_eu_identity {#sec-trust-der-cj-eu-identity}\n\n\n::: {#cell-fig-grf-trust-der-cj-eu-identity .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-trust-der-cj-eu-identity\n#| fig-cap: \"GRF CATE of the Muslim profile by der_cj_eu_identity (Trust): per-level subset ATE with honest cluster-robust 95% CI (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"der_cj_eu_identity\"\nlevs <- sort(unique(m_trust[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_trust[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_trust, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    level = as.character(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(\n    level = factor(level, levels = as.character(levs)),\n    lower = estimate - 1.96 * se,\n    upper = estimate + 1.96 * se\n  )\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = level)) +\n  geom_segment(\n    aes(xend = level, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_linerange(aes(ymin = lower, ymax = upper), colour = \"#0072B2\") +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr() +\n  theme(axis.text.x = element_text(angle = 30, hjust = 1))\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by der_cj_eu_identity (Trust): per-level subset ATE with honest cluster-robust 95% CI (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-trust-der-cj-eu-identity-1.png){#fig-grf-trust-der-cj-eu-identity width=3250}\n:::\n:::\n\n\n### der_cj_partisanship {#sec-trust-der-cj-partisanship}\n\n\n::: {#cell-fig-grf-trust-der-cj-partisanship .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-trust-der-cj-partisanship\n#| fig-cap: \"GRF CATE of the Muslim profile by der_cj_partisanship (Trust): per-level subset ATE with honest cluster-robust 95% CI (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"der_cj_partisanship\"\nlevs <- sort(unique(m_trust[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_trust[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_trust, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    level = as.character(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(\n    level = factor(level, levels = as.character(levs)),\n    lower = estimate - 1.96 * se,\n    upper = estimate + 1.96 * se\n  )\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = level)) +\n  geom_segment(\n    aes(xend = level, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_linerange(aes(ymin = lower, ymax = upper), colour = \"#0072B2\") +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr() +\n  theme(axis.text.x = element_text(angle = 30, hjust = 1))\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by der_cj_partisanship (Trust): per-level subset ATE with honest cluster-robust 95% CI (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-trust-der-cj-partisanship-1.png){#fig-grf-trust-der-cj-partisanship width=3250}\n:::\n:::\n\n\n### cj_age {#sec-trust-cj-age}\n\n\n::: {#cell-fig-grf-trust-cj-age .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-trust-cj-age\n#| fig-cap: \"GRF CATE of the Muslim profile by cj_age (Trust): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"cj_age\"\nlevs <- sort(unique(m_trust[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_trust[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_trust, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    value = as.numeric(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(lower = estimate - 1.96 * se, upper = estimate + 1.96 * se)\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = value)) +\n  geom_segment(\n    aes(xend = value, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = \"#0072B2\") +\n  geom_line(aes(y = estimate), colour = \"#0072B2\", linewidth = 0.8) +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by cj_age (Trust): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-trust-cj-age-1.png){#fig-grf-trust-cj-age width=3250}\n:::\n:::\n\n\n### cj_sex {#sec-trust-cj-sex}\n\n\n::: {#cell-fig-grf-trust-cj-sex .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-trust-cj-sex\n#| fig-cap: \"GRF CATE of the Muslim profile by cj_sex (Trust): per-level subset ATE with honest cluster-robust 95% CI (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"cj_sex\"\nlevs <- sort(unique(m_trust[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_trust[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_trust, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    level = as.character(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(\n    level = factor(level, levels = as.character(levs)),\n    lower = estimate - 1.96 * se,\n    upper = estimate + 1.96 * se\n  )\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = level)) +\n  geom_segment(\n    aes(xend = level, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_linerange(aes(ymin = lower, ymax = upper), colour = \"#0072B2\") +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr() +\n  theme(axis.text.x = element_text(angle = 30, hjust = 1))\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by cj_sex (Trust): per-level subset ATE with honest cluster-robust 95% CI (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-trust-cj-sex-1.png){#fig-grf-trust-cj-sex width=3250}\n:::\n:::\n\n\n### cj_class {#sec-trust-cj-class}\n\n\n::: {#cell-fig-grf-trust-cj-class .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-trust-cj-class\n#| fig-cap: \"GRF CATE of the Muslim profile by cj_class (Trust): per-level subset ATE with honest cluster-robust 95% CI (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"cj_class\"\nlevs <- sort(unique(m_trust[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_trust[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_trust, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    level = as.character(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(\n    level = factor(level, levels = as.character(levs)),\n    lower = estimate - 1.96 * se,\n    upper = estimate + 1.96 * se\n  )\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = level)) +\n  geom_segment(\n    aes(xend = level, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_linerange(aes(ymin = lower, ymax = upper), colour = \"#0072B2\") +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr() +\n  theme(axis.text.x = element_text(angle = 30, hjust = 1))\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by cj_class (Trust): per-level subset ATE with honest cluster-robust 95% CI (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-trust-cj-class-1.png){#fig-grf-trust-cj-class width=3250}\n:::\n:::\n\n\n## CATE by moderator — respondent-level {#sec-trust-resp}\n\n### q_gender {#sec-trust-q-gender}\n\n\n::: {#cell-fig-grf-trust-q-gender .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-trust-q-gender\n#| fig-cap: \"GRF CATE of the Muslim profile by q_gender (Trust): per-level subset ATE with honest cluster-robust 95% CI (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_gender\"\nlevs <- sort(unique(m_trust[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_trust[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_trust, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    level = as.character(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(\n    level = factor(level, levels = as.character(levs)),\n    lower = estimate - 1.96 * se,\n    upper = estimate + 1.96 * se\n  )\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = level)) +\n  geom_segment(\n    aes(xend = level, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_linerange(aes(ymin = lower, ymax = upper), colour = \"#0072B2\") +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr() +\n  theme(axis.text.x = element_text(angle = 30, hjust = 1))\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by q_gender (Trust): per-level subset ATE with honest cluster-robust 95% CI (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-trust-q-gender-1.png){#fig-grf-trust-q-gender width=3250}\n:::\n:::\n\n\n### q_age {#sec-trust-q-age}\n\n\n::: {#cell-fig-grf-trust-q-age .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-trust-q-age\n#| fig-cap: \"GRF CATE of the Muslim profile across deciles of q_age (Trust): per-bin subset ATE with honest cluster-robust 95% CI (ribbon); grey histogram shows the covariate distribution.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_age\"\ncol <- m_trust[[mod]]\nbin <- ntile(col, 10)\nrows <- list()\nfor (k in sort(unique(bin))) {\n  sel <- bin == k\n  a <- grf::average_treatment_effect(cf_trust, subset = sel)\n  rows[[as.character(k)]] <- tibble(\n    bin = k,\n    x_pos = mean(col[sel]),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(lower = estimate - 1.96 * se, upper = estimate + 1.96 * se)\n# distribution band\nyr <- range(c(cate$lower, cate$upper))\npad <- 0.12 * diff(yr)\nfloor_y <- yr[1] - pad\nspan <- 0.20 * diff(yr)\nh <- hist(col[!is.na(col)], breaks = 30, plot = FALSE)\nhd <- tibble(mid = h$mids, w = diff(h$breaks)[1], cnt = h$counts) |>\n  mutate(bar_top = floor_y + (cnt / max(cnt)) * span)\nggplot(cate, aes(x = x_pos)) +\n  geom_rect(\n    data = hd,\n    aes(xmin = mid - w / 2, xmax = mid + w / 2, ymin = floor_y, ymax = bar_top),\n    fill = \"grey80\",\n    inherit.aes = FALSE\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = \"#0072B2\") +\n  geom_line(aes(y = estimate), colour = \"#0072B2\", linewidth = 0.8) +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 1.5) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile across deciles of q_age (Trust): per-bin subset ATE with honest cluster-robust 95% CI (ribbon); grey histogram shows the covariate distribution.](07_postprocess_grf_files/figure-html/fig-grf-trust-q-age-1.png){#fig-grf-trust-q-age width=3250}\n:::\n:::\n\n\n### q_identity_country {#sec-trust-q-identity-country}\n\n\n::: {#cell-fig-grf-trust-q-identity-country .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-trust-q-identity-country\n#| fig-cap: \"GRF CATE of the Muslim profile by q_identity_country (Trust): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_identity_country\"\nlevs <- sort(unique(m_trust[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_trust[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_trust, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    value = as.numeric(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(lower = estimate - 1.96 * se, upper = estimate + 1.96 * se)\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = value)) +\n  geom_segment(\n    aes(xend = value, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = \"#0072B2\") +\n  geom_line(aes(y = estimate), colour = \"#0072B2\", linewidth = 0.8) +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by q_identity_country (Trust): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-trust-q-identity-country-1.png){#fig-grf-trust-q-identity-country width=3250}\n:::\n:::\n\n\n### q_identity_eu {#sec-trust-q-identity-eu}\n\n\n::: {#cell-fig-grf-trust-q-identity-eu .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-trust-q-identity-eu\n#| fig-cap: \"GRF CATE of the Muslim profile by q_identity_eu (Trust): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_identity_eu\"\nlevs <- sort(unique(m_trust[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_trust[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_trust, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    value = as.numeric(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(lower = estimate - 1.96 * se, upper = estimate + 1.96 * se)\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = value)) +\n  geom_segment(\n    aes(xend = value, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = \"#0072B2\") +\n  geom_line(aes(y = estimate), colour = \"#0072B2\", linewidth = 0.8) +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by q_identity_eu (Trust): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-trust-q-identity-eu-1.png){#fig-grf-trust-q-identity-eu width=3250}\n:::\n:::\n\n\n### q_identity_europe {#sec-trust-q-identity-europe}\n\n\n::: {#cell-fig-grf-trust-q-identity-europe .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-trust-q-identity-europe\n#| fig-cap: \"GRF CATE of the Muslim profile by q_identity_europe (Trust): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_identity_europe\"\nlevs <- sort(unique(m_trust[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_trust[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_trust, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    value = as.numeric(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(lower = estimate - 1.96 * se, upper = estimate + 1.96 * se)\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = value)) +\n  geom_segment(\n    aes(xend = value, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = \"#0072B2\") +\n  geom_line(aes(y = estimate), colour = \"#0072B2\", linewidth = 0.8) +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by q_identity_europe (Trust): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-trust-q-identity-europe-1.png){#fig-grf-trust-q-identity-europe width=3250}\n:::\n:::\n\n\n### q_religion {#sec-trust-q-religion}\n\n\n::: {#cell-fig-grf-trust-q-religion .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-trust-q-religion\n#| fig-cap: \"GRF CATE of the Muslim profile by q_religion (Trust): per-level subset ATE with honest cluster-robust 95% CI (blue); grey bars show per-level counts. Rare categories give wide CIs.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_religion\"\nlevs <- sort(unique(m_trust[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_trust[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_trust, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    level = as.character(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(\n    level = factor(level, levels = as.character(levs)),\n    lower = estimate - 1.96 * se,\n    upper = estimate + 1.96 * se\n  )\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = level)) +\n  geom_segment(\n    aes(xend = level, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_linerange(aes(ymin = lower, ymax = upper), colour = \"#0072B2\") +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr() +\n  theme(axis.text.x = element_text(angle = 30, hjust = 1))\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by q_religion (Trust): per-level subset ATE with honest cluster-robust 95% CI (blue); grey bars show per-level counts. Rare categories give wide CIs.](07_postprocess_grf_files/figure-html/fig-grf-trust-q-religion-1.png){#fig-grf-trust-q-religion width=3250}\n:::\n:::\n\n\n### q_class {#sec-trust-q-class}\n\n\n::: {#cell-fig-grf-trust-q-class .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-trust-q-class\n#| fig-cap: \"GRF CATE of the Muslim profile by q_class (Trust): per-level subset ATE with honest cluster-robust 95% CI (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_class\"\nlevs <- sort(unique(m_trust[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_trust[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_trust, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    level = as.character(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(\n    level = factor(level, levels = as.character(levs)),\n    lower = estimate - 1.96 * se,\n    upper = estimate + 1.96 * se\n  )\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = level)) +\n  geom_segment(\n    aes(xend = level, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_linerange(aes(ymin = lower, ymax = upper), colour = \"#0072B2\") +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr() +\n  theme(axis.text.x = element_text(angle = 30, hjust = 1))\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by q_class (Trust): per-level subset ATE with honest cluster-robust 95% CI (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-trust-q-class-1.png){#fig-grf-trust-q-class width=3250}\n:::\n:::\n\n\n### q_eu_efficacy_understand {#sec-trust-q-eu-efficacy-understand}\n\n\n::: {#cell-fig-grf-trust-q-eu-efficacy-understand .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-trust-q-eu-efficacy-understand\n#| fig-cap: \"GRF CATE of the Muslim profile by q_eu_efficacy_understand (Trust): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_eu_efficacy_understand\"\nlevs <- sort(unique(m_trust[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_trust[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_trust, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    value = as.numeric(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(lower = estimate - 1.96 * se, upper = estimate + 1.96 * se)\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = value)) +\n  geom_segment(\n    aes(xend = value, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = \"#0072B2\") +\n  geom_line(aes(y = estimate), colour = \"#0072B2\", linewidth = 0.8) +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by q_eu_efficacy_understand (Trust): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-trust-q-eu-efficacy-understand-1.png){#fig-grf-trust-q-eu-efficacy-understand width=3250}\n:::\n:::\n\n\n### q_pop_reps {#sec-trust-q-pop-reps}\n\n\n::: {#cell-fig-grf-trust-q-pop-reps .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-trust-q-pop-reps\n#| fig-cap: \"GRF CATE of the Muslim profile by q_pop_reps (Trust): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_pop_reps\"\nlevs <- sort(unique(m_trust[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_trust[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_trust, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    value = as.numeric(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(lower = estimate - 1.96 * se, upper = estimate + 1.96 * se)\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = value)) +\n  geom_segment(\n    aes(xend = value, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = \"#0072B2\") +\n  geom_line(aes(y = estimate), colour = \"#0072B2\", linewidth = 0.8) +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by q_pop_reps (Trust): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-trust-q-pop-reps-1.png){#fig-grf-trust-q-pop-reps width=3250}\n:::\n:::\n\n\n### q_pop_goodevil {#sec-trust-q-pop-goodevil}\n\n\n::: {#cell-fig-grf-trust-q-pop-goodevil .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-trust-q-pop-goodevil\n#| fig-cap: \"GRF CATE of the Muslim profile by q_pop_goodevil (Trust): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_pop_goodevil\"\nlevs <- sort(unique(m_trust[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_trust[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_trust, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    value = as.numeric(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(lower = estimate - 1.96 * se, upper = estimate + 1.96 * se)\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = value)) +\n  geom_segment(\n    aes(xend = value, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = \"#0072B2\") +\n  geom_line(aes(y = estimate), colour = \"#0072B2\", linewidth = 0.8) +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by q_pop_goodevil (Trust): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-trust-q-pop-goodevil-1.png){#fig-grf-trust-q-pop-goodevil width=3250}\n:::\n:::\n\n\n### q_pop_compromise {#sec-trust-q-pop-compromise}\n\n\n::: {#cell-fig-grf-trust-q-pop-compromise .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-trust-q-pop-compromise\n#| fig-cap: \"GRF CATE of the Muslim profile by q_pop_compromise (Trust): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_pop_compromise\"\nlevs <- sort(unique(m_trust[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_trust[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_trust, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    value = as.numeric(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(lower = estimate - 1.96 * se, upper = estimate + 1.96 * se)\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = value)) +\n  geom_segment(\n    aes(xend = value, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = \"#0072B2\") +\n  geom_line(aes(y = estimate), colour = \"#0072B2\", linewidth = 0.8) +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by q_pop_compromise (Trust): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-trust-q-pop-compromise-1.png){#fig-grf-trust-q-pop-compromise width=3250}\n:::\n:::\n\n\n### q_dem_compromise {#sec-trust-q-dem-compromise}\n\n\n::: {#cell-fig-grf-trust-q-dem-compromise .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-trust-q-dem-compromise\n#| fig-cap: \"GRF CATE of the Muslim profile by q_dem_compromise (Trust): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_dem_compromise\"\nlevs <- sort(unique(m_trust[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_trust[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_trust, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    value = as.numeric(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(lower = estimate - 1.96 * se, upper = estimate + 1.96 * se)\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = value)) +\n  geom_segment(\n    aes(xend = value, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = \"#0072B2\") +\n  geom_line(aes(y = estimate), colour = \"#0072B2\", linewidth = 0.8) +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by q_dem_compromise (Trust): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-trust-q-dem-compromise-1.png){#fig-grf-trust-q-dem-compromise width=3250}\n:::\n:::\n\n\n### q_dem_listen {#sec-trust-q-dem-listen}\n\n\n::: {#cell-fig-grf-trust-q-dem-listen .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-trust-q-dem-listen\n#| fig-cap: \"GRF CATE of the Muslim profile by q_dem_listen (Trust): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_dem_listen\"\nlevs <- sort(unique(m_trust[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_trust[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_trust, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    value = as.numeric(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(lower = estimate - 1.96 * se, upper = estimate + 1.96 * se)\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = value)) +\n  geom_segment(\n    aes(xend = value, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = \"#0072B2\") +\n  geom_line(aes(y = estimate), colour = \"#0072B2\", linewidth = 0.8) +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by q_dem_listen (Trust): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-trust-q-dem-listen-1.png){#fig-grf-trust-q-dem-listen width=3250}\n:::\n:::\n\n\n### q_tech_experts {#sec-trust-q-tech-experts}\n\n\n::: {#cell-fig-grf-trust-q-tech-experts .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-trust-q-tech-experts\n#| fig-cap: \"GRF CATE of the Muslim profile by q_tech_experts (Trust): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_tech_experts\"\nlevs <- sort(unique(m_trust[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_trust[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_trust, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    value = as.numeric(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(lower = estimate - 1.96 * se, upper = estimate + 1.96 * se)\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = value)) +\n  geom_segment(\n    aes(xend = value, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = \"#0072B2\") +\n  geom_line(aes(y = estimate), colour = \"#0072B2\", linewidth = 0.8) +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by q_tech_experts (Trust): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-trust-q-tech-experts-1.png){#fig-grf-trust-q-tech-experts width=3250}\n:::\n:::\n\n\n### q_tech_leaders {#sec-trust-q-tech-leaders}\n\n\n::: {#cell-fig-grf-trust-q-tech-leaders .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-trust-q-tech-leaders\n#| fig-cap: \"GRF CATE of the Muslim profile by q_tech_leaders (Trust): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_tech_leaders\"\nlevs <- sort(unique(m_trust[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_trust[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_trust, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    value = as.numeric(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(lower = estimate - 1.96 * se, upper = estimate + 1.96 * se)\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = value)) +\n  geom_segment(\n    aes(xend = value, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = \"#0072B2\") +\n  geom_line(aes(y = estimate), colour = \"#0072B2\", linewidth = 0.8) +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by q_tech_leaders (Trust): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-trust-q-tech-leaders-1.png){#fig-grf-trust-q-tech-leaders width=3250}\n:::\n:::\n\n\n### q_party_harm {#sec-trust-q-party-harm}\n\n\n::: {#cell-fig-grf-trust-q-party-harm .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-trust-q-party-harm\n#| fig-cap: \"GRF CATE of the Muslim profile by q_party_harm (Trust): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_party_harm\"\nlevs <- sort(unique(m_trust[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_trust[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_trust, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    value = as.numeric(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(lower = estimate - 1.96 * se, upper = estimate + 1.96 * se)\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = value)) +\n  geom_segment(\n    aes(xend = value, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = \"#0072B2\") +\n  geom_line(aes(y = estimate), colour = \"#0072B2\", linewidth = 0.8) +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by q_party_harm (Trust): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-trust-q-party-harm-1.png){#fig-grf-trust-q-party-harm width=3250}\n:::\n:::\n\n\n### q_people_incompetent {#sec-trust-q-people-incompetent}\n\n\n::: {#cell-fig-grf-trust-q-people-incompetent .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-trust-q-people-incompetent\n#| fig-cap: \"GRF CATE of the Muslim profile by q_people_incompetent (Trust): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_people_incompetent\"\nlevs <- sort(unique(m_trust[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_trust[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_trust, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    value = as.numeric(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(lower = estimate - 1.96 * se, upper = estimate + 1.96 * se)\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = value)) +\n  geom_segment(\n    aes(xend = value, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = \"#0072B2\") +\n  geom_line(aes(y = estimate), colour = \"#0072B2\", linewidth = 0.8) +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by q_people_incompetent (Trust): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-trust-q-people-incompetent-1.png){#fig-grf-trust-q-people-incompetent width=3250}\n:::\n:::\n\n\n### q_eu_longterm {#sec-trust-q-eu-longterm}\n\n\n::: {#cell-fig-grf-trust-q-eu-longterm .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-trust-q-eu-longterm\n#| fig-cap: \"GRF CATE of the Muslim profile by q_eu_longterm (Trust): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_eu_longterm\"\nlevs <- sort(unique(m_trust[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_trust[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_trust, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    value = as.numeric(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(lower = estimate - 1.96 * se, upper = estimate + 1.96 * se)\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = value)) +\n  geom_segment(\n    aes(xend = value, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = \"#0072B2\") +\n  geom_line(aes(y = estimate), colour = \"#0072B2\", linewidth = 0.8) +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by q_eu_longterm (Trust): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-trust-q-eu-longterm-1.png){#fig-grf-trust-q-eu-longterm width=3250}\n:::\n:::\n\n\n### q_eu_responsive {#sec-trust-q-eu-responsive}\n\n\n::: {#cell-fig-grf-trust-q-eu-responsive .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-trust-q-eu-responsive\n#| fig-cap: \"GRF CATE of the Muslim profile by q_eu_responsive (Trust): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_eu_responsive\"\nlevs <- sort(unique(m_trust[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_trust[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_trust, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    value = as.numeric(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(lower = estimate - 1.96 * se, upper = estimate + 1.96 * se)\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = value)) +\n  geom_segment(\n    aes(xend = value, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = \"#0072B2\") +\n  geom_line(aes(y = estimate), colour = \"#0072B2\", linewidth = 0.8) +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by q_eu_responsive (Trust): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-trust-q-eu-responsive-1.png){#fig-grf-trust-q-eu-responsive width=3250}\n:::\n:::\n\n\n### q_eu_imp_nat_econ {#sec-trust-q-eu-imp-nat-econ}\n\n\n::: {#cell-fig-grf-trust-q-eu-imp-nat-econ .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-trust-q-eu-imp-nat-econ\n#| fig-cap: \"GRF CATE of the Muslim profile by q_eu_imp_nat_econ (Trust): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_eu_imp_nat_econ\"\nlevs <- sort(unique(m_trust[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_trust[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_trust, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    value = as.numeric(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(lower = estimate - 1.96 * se, upper = estimate + 1.96 * se)\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = value)) +\n  geom_segment(\n    aes(xend = value, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = \"#0072B2\") +\n  geom_line(aes(y = estimate), colour = \"#0072B2\", linewidth = 0.8) +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by q_eu_imp_nat_econ (Trust): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-trust-q-eu-imp-nat-econ-1.png){#fig-grf-trust-q-eu-imp-nat-econ width=3250}\n:::\n:::\n\n\n### q_eu_imp_nat_cul {#sec-trust-q-eu-imp-nat-cul}\n\n\n::: {#cell-fig-grf-trust-q-eu-imp-nat-cul .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-trust-q-eu-imp-nat-cul\n#| fig-cap: \"GRF CATE of the Muslim profile by q_eu_imp_nat_cul (Trust): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_eu_imp_nat_cul\"\nlevs <- sort(unique(m_trust[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_trust[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_trust, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    value = as.numeric(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(lower = estimate - 1.96 * se, upper = estimate + 1.96 * se)\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = value)) +\n  geom_segment(\n    aes(xend = value, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = \"#0072B2\") +\n  geom_line(aes(y = estimate), colour = \"#0072B2\", linewidth = 0.8) +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by q_eu_imp_nat_cul (Trust): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-trust-q-eu-imp-nat-cul-1.png){#fig-grf-trust-q-eu-imp-nat-cul width=3250}\n:::\n:::\n\n\n### q_eu_imp_nat_pol {#sec-trust-q-eu-imp-nat-pol}\n\n\n::: {#cell-fig-grf-trust-q-eu-imp-nat-pol .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-trust-q-eu-imp-nat-pol\n#| fig-cap: \"GRF CATE of the Muslim profile by q_eu_imp_nat_pol (Trust): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_eu_imp_nat_pol\"\nlevs <- sort(unique(m_trust[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_trust[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_trust, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    value = as.numeric(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(lower = estimate - 1.96 * se, upper = estimate + 1.96 * se)\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = value)) +\n  geom_segment(\n    aes(xend = value, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = \"#0072B2\") +\n  geom_line(aes(y = estimate), colour = \"#0072B2\", linewidth = 0.8) +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by q_eu_imp_nat_pol (Trust): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-trust-q-eu-imp-nat-pol-1.png){#fig-grf-trust-q-eu-imp-nat-pol width=3250}\n:::\n:::\n\n\n### q_eu_abolish {#sec-trust-q-eu-abolish}\n\n\n::: {#cell-fig-grf-trust-q-eu-abolish .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-trust-q-eu-abolish\n#| fig-cap: \"GRF CATE of the Muslim profile by q_eu_abolish (Trust): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_eu_abolish\"\nlevs <- sort(unique(m_trust[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_trust[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_trust, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    value = as.numeric(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(lower = estimate - 1.96 * se, upper = estimate + 1.96 * se)\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = value)) +\n  geom_segment(\n    aes(xend = value, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = \"#0072B2\") +\n  geom_line(aes(y = estimate), colour = \"#0072B2\", linewidth = 0.8) +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by q_eu_abolish (Trust): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-trust-q-eu-abolish-1.png){#fig-grf-trust-q-eu-abolish width=3250}\n:::\n:::\n\n\n### q_eu_satisfaction {#sec-trust-q-eu-satisfaction}\n\n\n::: {#cell-fig-grf-trust-q-eu-satisfaction .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-trust-q-eu-satisfaction\n#| fig-cap: \"GRF CATE of the Muslim profile by q_eu_satisfaction (Trust): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_eu_satisfaction\"\nlevs <- sort(unique(m_trust[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_trust[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_trust, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    value = as.numeric(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(lower = estimate - 1.96 * se, upper = estimate + 1.96 * se)\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = value)) +\n  geom_segment(\n    aes(xend = value, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = \"#0072B2\") +\n  geom_line(aes(y = estimate), colour = \"#0072B2\", linewidth = 0.8) +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by q_eu_satisfaction (Trust): connected per-level subset ATE with honest cluster-robust 95% CI ribbon (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-trust-q-eu-satisfaction-1.png){#fig-grf-trust-q-eu-satisfaction width=3250}\n:::\n:::\n\n\n### q_rural_urban {#sec-trust-q-rural-urban}\n\n\n::: {#cell-fig-grf-trust-q-rural-urban .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-trust-q-rural-urban\n#| fig-cap: \"GRF CATE of the Muslim profile by q_rural_urban (Trust): per-level subset ATE with honest cluster-robust 95% CI (blue); grey bars show per-level counts.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_rural_urban\"\nlevs <- sort(unique(m_trust[[mod]]))\nrows <- list()\nfor (lev in levs) {\n  sel <- m_trust[[mod]] == lev\n  a <- grf::average_treatment_effect(cf_trust, subset = sel)\n  rows[[as.character(lev)]] <- tibble(\n    level = as.character(lev),\n    n = sum(sel),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(\n    level = factor(level, levels = as.character(levs)),\n    lower = estimate - 1.96 * se,\n    upper = estimate + 1.96 * se\n  )\nyr <- range(c(cate$lower, cate$upper))\npad <- if (diff(yr) > 0) 0.12 * diff(yr) else 0.5\nfloor_y <- yr[1] - pad\nspan <- if (diff(yr) > 0) 0.20 * diff(yr) else 0.5\ncate <- cate |> mutate(bar_top = floor_y + (n / max(n)) * span)\nggplot(cate, aes(x = level)) +\n  geom_segment(\n    aes(xend = level, y = floor_y, yend = bar_top),\n    colour = \"grey80\",\n    linewidth = 4\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_linerange(aes(ymin = lower, ymax = upper), colour = \"#0072B2\") +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 2) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr() +\n  theme(axis.text.x = element_text(angle = 30, hjust = 1))\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile by q_rural_urban (Trust): per-level subset ATE with honest cluster-robust 95% CI (blue); grey bars show per-level counts.](07_postprocess_grf_files/figure-html/fig-grf-trust-q-rural-urban-1.png){#fig-grf-trust-q-rural-urban width=3250}\n:::\n:::\n\n\n### q_edu_age_stop {#sec-trust-q-edu-age-stop}\n\n\n::: {#cell-fig-grf-trust-q-edu-age-stop .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-trust-q-edu-age-stop\n#| fig-cap: \"GRF CATE of the Muslim profile across deciles of q_edu_age_stop (Trust): per-bin subset ATE with honest cluster-robust 95% CI (ribbon); grey histogram shows the covariate distribution.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\nmod <- \"q_edu_age_stop\"\ncol <- m_trust[[mod]]\nbin <- ntile(col, 10)\nrows <- list()\nfor (k in sort(unique(bin))) {\n  sel <- bin == k\n  a <- grf::average_treatment_effect(cf_trust, subset = sel)\n  rows[[as.character(k)]] <- tibble(\n    bin = k,\n    x_pos = mean(col[sel]),\n    estimate = a[\"estimate\"],\n    se = a[\"std.err\"]\n  )\n}\ncate <- bind_rows(rows) |>\n  mutate(lower = estimate - 1.96 * se, upper = estimate + 1.96 * se)\n# distribution band\nyr <- range(c(cate$lower, cate$upper))\npad <- 0.12 * diff(yr)\nfloor_y <- yr[1] - pad\nspan <- 0.20 * diff(yr)\nh <- hist(col[!is.na(col)], breaks = 30, plot = FALSE)\nhd <- tibble(mid = h$mids, w = diff(h$breaks)[1], cnt = h$counts) |>\n  mutate(bar_top = floor_y + (cnt / max(cnt)) * span)\nggplot(cate, aes(x = x_pos)) +\n  geom_rect(\n    data = hd,\n    aes(xmin = mid - w / 2, xmax = mid + w / 2, ymin = floor_y, ymax = bar_top),\n    fill = \"grey80\",\n    inherit.aes = FALSE\n  ) +\n  geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_ribbon(aes(ymin = lower, ymax = upper), alpha = 0.2, fill = \"#0072B2\") +\n  geom_line(aes(y = estimate), colour = \"#0072B2\", linewidth = 0.8) +\n  geom_point(aes(y = estimate), colour = \"#0072B2\", size = 1.5) +\n  labs(title = paste0(\"CATE by \", mod), x = mod, y = yl) +\n  ggpubr::theme_pubr()\n```\n\n::: {.cell-output-display}\n![GRF CATE of the Muslim profile across deciles of q_edu_age_stop (Trust): per-bin subset ATE with honest cluster-robust 95% CI (ribbon); grey histogram shows the covariate distribution.](07_postprocess_grf_files/figure-html/fig-grf-trust-q-edu-age-stop-1.png){#fig-grf-trust-q-edu-age-stop width=3250}\n:::\n:::\n\n\n## Combined figures {#sec-combined}\n\nThe per-game, per-moderator figures above are the detailed view.\nThis section attempts two compact overviews that place every moderator on one canvas and\noverlay both games at once.\nFollowing the house style of `09_additional_figs_tables.qmd`, the two games are the only thing\nencoded by colour, shape, and linetype, and they are always encoded by all three together, so\neach game is distinguishable in colour, in greyscale print, and under colourblindness.\nEvery other distinction (which moderator, which level) is carried by facets and axis position.\n\n## House style {#sec-combined-house}\n\nThe Okabe-Ito game palette with redundant shape and linetype, the three reserved scales, the\nshared theme, and the golden-ratio helper, copied from `09_additional_figs_tables.qmd`.\n\n\n::: {.cell}\n\n```{.r .cell-code .hidden}\n#| label: game-style\ngame_labs <- c(cj_dict = \"Dictator game\", cj_trust = \"Trust game\")\ngame_cols <- c(\"Dictator game\" = \"#0072B2\", \"Trust game\" = \"#D55E00\")\ngame_shapes <- c(\"Dictator game\" = 16, \"Trust game\" = 17) # circle / triangle\ngame_ltys <- c(\"Dictator game\" = \"solid\", \"Trust game\" = \"longdash\")\n\nscale_colour_game <- function() scale_colour_manual(values = game_cols, name = NULL)\nscale_shape_game <- function() scale_shape_manual(values = game_shapes, name = NULL)\nscale_linetype_game <- function() scale_linetype_manual(values = game_ltys, name = NULL)\n\n# shared moderator labels + categorical-first ordering for the facets\nsource(here::here(\"code\", \"helper_scripts\", \"moderator_labels.R\"))\n\ntheme_mbeu <- function(base_size = 11) {\n  ggpubr::theme_pubr(base_size = base_size) +\n    theme(legend.position = \"top\")\n}\nref_zero <- geom_hline(yintercept = 0, linetype = \"dashed\", colour = \"grey60\")\ndodge <- position_dodge(width = 0.5)\nyl_cate <- expression(widehat(CATE))\nyl_ric <- expression(hat(tau)[ric]) # profile-level CATE\nyl_i <- expression(hat(tau)[ic]) # respondent-level CATE\n```\n:::\n\n\n## Presentational moderator transforms {#sec-combined-transforms}\n\nTwo display-only recodings, applied to the plotting copies `m_dict` / `m_trust` only.\nThe forest design matrices `X_dict` / `X_trust` were built far above, so the best linear\nprojection and variable-importance figures are untouched; only the CATE-by-moderator panels\nbelow see these.\nThe three identity scales run `1` (most attached) to `5` (least attached); we reverse them to\n`6 - x` so that, like every other panel, moving right means *more* of the construct (more\nattachment).\nThe three \"EU effect on\" items (`q_eu_imp_nat_econ`, `q_eu_imp_nat_cul`, `q_eu_imp_nat_pol`) run\n`1` (very positive) to `5` (very negative); we reverse them the same way so moving right means a\n*more positive* evaluation of the EU's effect.\n`q_class` is reordered from working class up to higher class, with the non-substantive\n`dont_know` last, so the facet reads as a status gradient rather than alphabetically.\n\n\n::: {.cell}\n\n```{.r .cell-code .hidden}\n#| label: combined-transforms\nclass_order <- c(\n  \"working_class\", \"lower_middle_class\", \"middle_class\",\n  \"upper_middle_class\", \"higher_class\", \"dont_know\", \"other\"\n)\nflip5 <- \\(x) 6 - x # 5-point scales: reverse so higher = more of the construct\nfor (nm in c(\"m_dict\", \"m_trust\")) {\n  assign(nm, get(nm) |>\n    mutate(\n      across(\n        c(\n          q_identity_country, q_identity_eu, q_identity_europe,\n          q_eu_imp_nat_econ, q_eu_imp_nat_cul, q_eu_imp_nat_pol\n        ),\n        flip5\n      ),\n      q_class = factor(q_class, levels = class_order),\n      q_rural_urban = factor(\n        q_rural_urban,\n        levels = c(\"rural\", \"small_med_town\", \"large_city\")\n      ),\n      der_cj_eu_identity = factor(\n        der_cj_eu_identity,\n        levels = c(\"eu_citizen\", \"not_eu_citizen\", \"not_displayed\")\n      ),\n      der_cj_partisanship = factor(\n        der_cj_partisanship,\n        levels = c(\"not_shown\", \"shown\", \"not_applicable\")\n      )\n    ))\n}\n```\n:::\n\n\n## Builders {#sec-combined-build}\n\n`cate_one()` returns the per-level (or per-grid-cell, for the two genuinely continuous moderators)\nsubset ATE and 95% CI for one moderator in one game, tagging whether the moderator is an ordered\nnumeric scale (which earns a connecting line) and carrying the per-level count `n`.\nDiscrete levels comprising fewer than 1% of that game's sample are dropped, because their subset\nCATE is too imprecisely estimated to compare against the well-populated levels; `cj_age` is treated\nas discrete (points only, no line) because its five values are fixed conjoint ages, not a\ncontinuous scale.\nThe two genuinely continuous moderators are binned on a **fixed display grid** rather than into\ndeciles — `q_age` over 100 equal intervals across 18–75, `q_edu_age_stop` at each integer year\n15–30 — so the line spans the full range (decile bins were plotted at their bin *means*, which is\nwhy the old `q_age` line began near 25 rather than 18); the 1% rule is not applied to these grid\ncells, since the full range is shown by design.\n`build_comb()` stacks that over a set of moderators and both games.\n`plot_comb()` draws the faceted overview with a free y-axis per facet: grey per-level count bars\n(pooled over the games shown) along the bottom, points and CIs dodged by game everywhere, plus a\nconnecting line on the ordered-scale facets only.\n\n\n::: {.cell}\n\n```{.r .cell-code .hidden}\n#| label: combined-builders\n# fixed display grids for the two genuinely continuous moderators\ncont_grids <- list(\n  q_age = seq(18, 75, length.out = 101), # 100 intervals over the full range\n  q_edu_age_stop = seq(14.5, 30.5, by = 1) # integer years 15..30\n)\n\ncate_one <- function(cf, mm, mod, game_label) {\n  v <- mm[[mod]]\n  is_num <- is.numeric(v)\n  # genuinely continuous moderators\n  if (is_num && length(unique(v)) > 12) {\n    # fixed-grid moderators: subset ATE per non-empty grid cell, plotted at the\n    # cell midpoint so the line spans the full display range\n    if (mod %in% names(cont_grids)) {\n      br <- cont_grids[[mod]]\n      cell <- cut(v, breaks = br, include.lowest = TRUE)\n      mids <- (head(br, -1) + tail(br, -1)) / 2\n      rows <- lapply(which(tabulate(cell, nbins = length(mids)) > 0), function(k) {\n        sel <- !is.na(cell) & as.integer(cell) == k\n        a <- grf::average_treatment_effect(cf, subset = sel)\n        tibble(\n          mod = mod, level = as.character(round(mids[k])),\n          order = mids[k], is_scale = TRUE, game = game_label,\n          n = sum(sel),\n          estimate = a[\"estimate\"],\n          lower = a[\"estimate\"] - 1.96 * a[\"std.err\"],\n          upper = a[\"estimate\"] + 1.96 * a[\"std.err\"]\n        )\n      })\n      return(bind_rows(rows))\n    }\n    # fallback: decile bins, integer labels\n    bin <- ntile(v, 10)\n    rows <- lapply(sort(unique(bin)), function(k) {\n      sel <- bin == k\n      a <- grf::average_treatment_effect(cf, subset = sel)\n      tibble(\n        mod = mod, level = as.character(round(mean(v[sel]))),\n        order = mean(v[sel]), is_scale = TRUE, game = game_label,\n        n = sum(sel),\n        estimate = a[\"estimate\"],\n        lower = a[\"estimate\"] - 1.96 * a[\"std.err\"],\n        upper = a[\"estimate\"] + 1.96 * a[\"std.err\"]\n      )\n    })\n    return(bind_rows(rows))\n  }\n  # discrete moderators: honor factor level order; drop levels below 1% of the game sample\n  levs <- if (is_num) {\n    sort(unique(v))\n  } else if (is.factor(v)) {\n    levels(droplevels(v))\n  } else {\n    sort(unique(as.character(v)))\n  }\n  rows <- lapply(seq_along(levs), function(i) {\n    lev <- levs[i]\n    sel <- if (is_num) v == lev else as.character(v) == lev\n    # drop rare levels (< 1% of the game sample): subset CATE too imprecise to compare\n    if (sum(sel) / sum(!is.na(v)) < 0.01) {\n      return(NULL)\n    }\n    a <- grf::average_treatment_effect(cf, subset = sel)\n    tibble(\n      mod = mod, level = as.character(lev),\n      order = if (is_num) as.numeric(lev) else i,\n      is_scale = is_num && mod != \"cj_age\", game = game_label,\n      n = sum(sel),\n      estimate = a[\"estimate\"],\n      lower = a[\"estimate\"] - 1.96 * a[\"std.err\"],\n      upper = a[\"estimate\"] + 1.96 * a[\"std.err\"]\n    )\n  })\n  bind_rows(rows)\n}\n\nbuild_comb <- function(mods) {\n  bind_rows(\n    bind_rows(lapply(mods, \\(m) cate_one(cf_dict, m_dict, m, \"Dictator game\"))),\n    bind_rows(lapply(mods, \\(m) cate_one(cf_trust, m_trust, m, \"Trust game\")))\n  ) |>\n    mutate(mod = factor(mod, levels = mods))\n}\n\nplot_comb <- function(df, ncol, title, ylab = yl_i) {\n  d <- df |>\n    arrange(mod, order) |>\n    mutate(key = factor(\n      paste0(mod, \"@@\", level),\n      levels = unique(paste0(mod, \"@@\", level))\n    ))\n  # per-facet grey count bars (pooled over games shown)\n  band <- d |>\n    summarise(cnt = sum(n), .by = c(mod, key)) |>\n    left_join(\n      d |>\n        summarise(\n          floor_y = min(lower) - 0.12 * (max(upper) - min(lower)),\n          span = 0.20 * (max(upper) - min(lower)),\n          .by = mod\n        ),\n      by = \"mod\"\n    ) |>\n    mutate(bar_top = floor_y + (cnt / max(cnt)) * span, .by = mod)\n  d_scale <- dplyr::filter(d, is_scale)\n  d_cat <- dplyr::filter(d, !is_scale)\n  p <- ggplot(d, aes(x = key, y = estimate, colour = game, shape = game)) +\n    geom_segment(\n      data = band,\n      aes(x = key, xend = key, y = floor_y, yend = bar_top),\n      colour = \"grey80\",\n      linewidth = 3,\n      inherit.aes = FALSE\n    ) +\n    ref_zero\n  # ordered scales: overlaid ribbon + line + point (continuous-style)\n  if (nrow(d_scale) > 0) {\n    p <- p +\n      geom_ribbon(\n        data = d_scale,\n        aes(ymin = lower, ymax = upper, fill = game, group = game),\n        alpha = 0.15,\n        colour = NA\n      ) +\n      geom_line(data = d_scale, aes(group = game, linetype = game)) +\n      geom_point(data = d_scale) +\n      scale_linetype_game() +\n      scale_fill_manual(values = game_cols, name = NULL)\n  }\n  # categorical: dodged linerange + point\n  if (nrow(d_cat) > 0) {\n    p <- p +\n      geom_linerange(\n        data = d_cat,\n        aes(ymin = lower, ymax = upper),\n        position = dodge\n      ) +\n      geom_point(data = d_cat, position = dodge)\n  }\n  p +\n    facet_wrap(~mod, scales = \"free\", ncol = ncol, labeller = labeller(mod = mod_strip)) +\n    scale_colour_game() +\n    scale_shape_game() +\n    scale_x_discrete(labels = relabel_key) +\n    labs(title = title, x = NULL, y = ylab) +\n    guides(fill = \"none\", linetype = \"none\") +\n    theme_mbeu() +\n    theme(axis.text.x = element_text(angle = 45, hjust = 1, size = 7))\n}\n\n# one continuous moderator: fixed-grid CATE ribbon + real histogram, x trimmed to xlim\ncont_panel <- function(mod, xlim, ylab = yl_i) {\n  d <- build_comb(mod)\n  # covariate distribution within the visible range\n  v <- m_dict[[mod]]\n  v <- v[!is.na(v) & v >= xlim[1] & v <= xlim[2]]\n  h <- hist(v, breaks = 30, plot = FALSE)\n  floor_y <- min(d$lower) - 0.12 * (max(d$upper) - min(d$lower))\n  span <- 0.20 * (max(d$upper) - min(d$lower))\n  hd <- tibble(mid = h$mids, w = diff(h$breaks)[1], cnt = h$counts) |>\n    mutate(bar_top = floor_y + (cnt / max(cnt)) * span)\n  ggplot(d, aes(x = order, y = estimate, colour = game, shape = game)) +\n    geom_rect(\n      data = hd,\n      aes(xmin = mid - w / 2, xmax = mid + w / 2, ymin = floor_y, ymax = bar_top),\n      fill = \"grey80\",\n      inherit.aes = FALSE\n    ) +\n    ref_zero +\n    geom_ribbon(\n      aes(ymin = lower, ymax = upper, fill = game, group = game),\n      alpha = 0.15,\n      colour = NA\n    ) +\n    geom_line(aes(group = game, linetype = game)) +\n    geom_point() +\n    scale_colour_game() +\n    scale_shape_game() +\n    scale_linetype_game() +\n    scale_fill_manual(values = game_cols, name = NULL) +\n    scale_x_continuous(breaks = scales::breaks_pretty(6)) +\n    coord_cartesian(xlim = xlim) +\n    labs(title = mod_labels[[mod]], x = \"Covariate value (years)\", y = ylab) +\n    theme_mbeu()\n}\n```\n:::\n\n\n## Conjoint-round moderators, both games {#sec-combined-prof}\n\nAll five conjoint-round (profile-level) moderators on one canvas, Dictator and Trust overlaid.\nAll five are categorical (including `cj_age`, whose five values are fixed conjoint ages), so each\nis drawn as dodged points only.\n\n\n::: {#cell-fig-grf-combined-prof .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-combined-prof\n#| fig-cap: \"CF estimates of the profile-level CATE of the Muslim profile across the levels of every conjoint-round moderator (x-axis), Dictator and Trust games overlaid and distinguished jointly by colour, shape, and linetype. The y-axis ($\\\\hat{\\\\tau}_{ric}$) is the profile-level CATE on the token-allocation scale. Points are per-level doubly-robust subset estimates and the vertical bars are honest cluster-robust 95% confidence intervals (each a separate cluster-robust estimate on that subset, clustered by respondent). Grey bars along the bottom of each facet show the per-level row count (pooled over games). The y-axis is free per facet. Levels comprising less than 1% of a game's sample are dropped because their subset estimate is too imprecise to compare. In the Profile partisanship facet, partisanship not shown refers to within the co-national category only, since partisanship is displayed for co-national profiles only.\"\n#| dpi: 500\n#| fig-width: 9\n#| fig-height: 6\ncomb_prof <- build_comb(prof_mods)\nplot_comb(\n  comb_prof,\n  ncol = 3,\n  title = bquote(\"CATE by conjoint-round moderator, both games\" ~ (hat(tau)[ric])),\n  ylab = yl_ric\n)\n```\n\n::: {.cell-output-display}\n![CF estimates of the profile-level CATE of the Muslim profile across the levels of every conjoint-round moderator (x-axis), Dictator and Trust games overlaid and distinguished jointly by colour, shape, and linetype. The y-axis ($\\hat{\\tau}_{ric}$) is the profile-level CATE on the token-allocation scale. Points are per-level doubly-robust subset estimates and the vertical bars are honest cluster-robust 95% confidence intervals (each a separate cluster-robust estimate on that subset, clustered by respondent). Grey bars along the bottom of each facet show the per-level row count (pooled over games). The y-axis is free per facet. Levels comprising less than 1% of a game's sample are dropped because their subset estimate is too imprecise to compare. In the Profile partisanship facet, partisanship not shown refers to within the co-national category only, since partisanship is displayed for co-national profiles only.](07_postprocess_grf_files/figure-html/fig-grf-combined-prof-1.png){#fig-grf-combined-prof width=4500}\n:::\n:::\n\n\n## Individual-level moderators, both games {#sec-combined-resp}\n\nThe respondent-level moderators on two figures so the facets stay legible.\nEach figure stacks (via `patchwork`) a faceted panel of half the discrete moderators on top with\none of the two genuinely continuous moderators (`q_age`, `q_edu_age_stop`) on a continuous x-axis\nbelow, so the continuous covariate keeps its real histogram and trimmed axis while still sitting\nbeside the discrete moderators it belongs with.\nOrdered numeric scales (the 5- and 7-point items) now carry the same ribbon styling as the\ncontinuous panels — an overlaid 95% CI band plus connecting line per game; categorical moderators\nkeep dodged points and CIs.\nThe three identity scales (`q_identity_country`, `q_identity_eu`, `q_identity_europe`) are reversed\nfor display so higher on the x-axis means *more* attachment, the three \"EU effect on\" items\n(`q_eu_imp_nat_econ`, `q_eu_imp_nat_cul`, `q_eu_imp_nat_pol`) are reversed so higher means a *more\npositive* evaluation, and `q_class` is ordered from working class up to higher class with\n`dont_know` last.\nThe shared game legend is drawn once, from the top panel.\n\n\n::: {.cell}\n\n```{.r .cell-code .hidden}\n#| label: resp-split\nresp_cat <- setdiff(resp_mods, c(\"q_age\", \"q_edu_age_stop\"))\n# categorical moderators first, then the ordered scales\nresp_cat <- c(intersect(cat_mods, resp_cat), setdiff(resp_cat, cat_mods))\nhalf <- ceiling(length(resp_cat) / 2)\nresp_half1 <- resp_cat[1:half]\nresp_half2 <- resp_cat[(half + 1):length(resp_cat)]\n```\n:::\n\n\n\n::: {#cell-fig-grf-combined-resp-1 .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-combined-resp-1\n#| fig-cap: \"CF estimates of the respondent-level CATE of the Muslim profile across the levels of the first half of the discrete individual-level moderators (top panel) and across age (q_age, bottom panel), Dictator and Trust games overlaid and distinguished jointly by colour, shape, and linetype. The y-axis ($\\\\hat{\\\\tau}_i$) is the respondent-level CATE on the token-allocation scale. Identity scales are reversed so higher means more attachment. The EU-effect items are reversed so higher means a more positive evaluation, and q_class runs working class up to higher class with dont_know last. Top: points are per-level doubly-robust subset estimates with honest cluster-robust 95% confidence intervals (each a separate cluster-robust estimate on that subset, clustered by respondent). Ordered numeric scales are drawn as a connecting line and 95% CI ribbon, categorical moderators as dodged points and CIs. Grey bars show the per-level row count (pooled over games). Bottom: per-grid-cell subset estimate, 95% CI ribbon, and connecting line per game over a grey histogram of the covariate, on 100 equal intervals across the full 18–75 years. The y-axis is free per facet. Discrete levels comprising less than 1% of a game's sample are dropped because their subset estimate is too imprecise to compare.\"\n#| dpi: 500\n#| fig-width: 11\n#| fig-height: 14\ncomb_resp1 <- build_comb(resp_half1)\np_resp1 <- plot_comb(\n  comb_resp1,\n  ncol = 4,\n  title = bquote(\n    \"CATE by individual-level moderator, both games (1 of 2)\" ~ (hat(tau)[ic])\n  )\n)\np_age <- cont_panel(\"q_age\", c(18, 75)) +\n  guides(colour = \"none\", shape = \"none\", fill = \"none\", linetype = \"none\")\n(p_resp1 / p_age) + plot_layout(heights = c(3, 1))\n```\n\n::: {.cell-output-display}\n![CF estimates of the respondent-level CATE of the Muslim profile across the levels of the first half of the discrete individual-level moderators (top panel) and across age (q_age, bottom panel), Dictator and Trust games overlaid and distinguished jointly by colour, shape, and linetype. The y-axis ($\\hat{\\tau}_i$) is the respondent-level CATE on the token-allocation scale. Identity scales are reversed so higher means more attachment. The EU-effect items are reversed so higher means a more positive evaluation, and q_class runs working class up to higher class with dont_know last. Top: points are per-level doubly-robust subset estimates with honest cluster-robust 95% confidence intervals (each a separate cluster-robust estimate on that subset, clustered by respondent). Ordered numeric scales are drawn as a connecting line and 95% CI ribbon, categorical moderators as dodged points and CIs. Grey bars show the per-level row count (pooled over games). Bottom: per-grid-cell subset estimate, 95% CI ribbon, and connecting line per game over a grey histogram of the covariate, on 100 equal intervals across the full 18–75 years. The y-axis is free per facet. Discrete levels comprising less than 1% of a game's sample are dropped because their subset estimate is too imprecise to compare.](07_postprocess_grf_files/figure-html/fig-grf-combined-resp-1-1.png){#fig-grf-combined-resp-1 width=5500}\n:::\n:::\n\n\n\n::: {#cell-fig-grf-combined-resp-2 .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-combined-resp-2\n#| fig-cap: \"CF estimates of the respondent-level CATE of the Muslim profile across the levels of the second half of the discrete individual-level moderators (top panel) and across age at which education stopped (q_edu_age_stop, bottom panel), Dictator and Trust games overlaid and distinguished jointly by colour, shape, and linetype. The y-axis ($\\\\hat{\\\\tau}_i$) is the respondent-level CATE on the token-allocation scale. Identity scales are reversed so higher means more attachment. The EU-effect items are reversed so higher means a more positive evaluation, and q_class runs working class up to higher class with dont_know last. Top: points are per-level doubly-robust subset estimates with honest cluster-robust 95% confidence intervals (each a separate cluster-robust estimate on that subset, clustered by respondent). Ordered numeric scales are drawn as a connecting line and 95% CI ribbon, categorical moderators as dodged points and CIs. Grey bars show the per-level row count (pooled over games). Bottom: subset estimate, 95% CI ribbon, and connecting line per game at each integer year of education-stopping age from 15 to 30, over a grey histogram of the covariate. The y-axis is free per facet. Discrete levels comprising less than 1% of a game's sample are dropped because their subset estimate is too imprecise to compare.\"\n#| dpi: 500\n#| fig-width: 11\n#| fig-height: 14\ncomb_resp2 <- build_comb(resp_half2)\np_resp2 <- plot_comb(\n  comb_resp2,\n  ncol = 4,\n  title = bquote(\n    \"CATE by individual-level moderator, both games (2 of 2)\" ~ (hat(tau)[ic])\n  )\n)\np_edu <- cont_panel(\"q_edu_age_stop\", c(15, 30)) +\n  guides(colour = \"none\", shape = \"none\", fill = \"none\", linetype = \"none\")\n(p_resp2 / p_edu) + plot_layout(heights = c(3, 1))\n```\n\n::: {.cell-output-display}\n![CF estimates of the respondent-level CATE of the Muslim profile across the levels of the second half of the discrete individual-level moderators (top panel) and across age at which education stopped (q_edu_age_stop, bottom panel), Dictator and Trust games overlaid and distinguished jointly by colour, shape, and linetype. The y-axis ($\\hat{\\tau}_i$) is the respondent-level CATE on the token-allocation scale. Identity scales are reversed so higher means more attachment. The EU-effect items are reversed so higher means a more positive evaluation, and q_class runs working class up to higher class with dont_know last. Top: points are per-level doubly-robust subset estimates with honest cluster-robust 95% confidence intervals (each a separate cluster-robust estimate on that subset, clustered by respondent). Ordered numeric scales are drawn as a connecting line and 95% CI ribbon, categorical moderators as dodged points and CIs. Grey bars show the per-level row count (pooled over games). Bottom: subset estimate, 95% CI ribbon, and connecting line per game at each integer year of education-stopping age from 15 to 30, over a grey histogram of the covariate. The y-axis is free per facet. Discrete levels comprising less than 1% of a game's sample are dropped because their subset estimate is too imprecise to compare.](07_postprocess_grf_files/figure-html/fig-grf-combined-resp-2-1.png){#fig-grf-combined-resp-2 width=5500}\n:::\n:::\n\n\n## Diagnostics, both games {#sec-combined-diag}\n\nThe three per-game diagnostics — the TOC/AUTOC curve, the best linear projection, and the\nconstruct-level variable importance — placed on one canvas each with the two games overlaid,\nreusing the objects already computed in the per-game sections above.\nGame is carried by colour (and, where a geom supports it, shape and linetype); every other\ndistinction is carried by axis position or facet.\n\n\n::: {#cell-fig-grf-combined-toc .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-combined-toc\n#| fig-cap: \"CF Targeting Operator Characteristic curve, both games overlaid: ATE among the top-q fraction by predicted CATE minus the overall ATE, with 95% CI ribbon. The area under each curve is the AUTOC, reported in the title.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 4\ntoc_comb <- bind_rows(\n  toc_dict |> mutate(game = \"Dictator game\"),\n  toc_trust |> mutate(game = \"Trust game\")\n) |>\n  mutate(game = factor(game, levels = c(\"Dictator game\", \"Trust game\")))\n\nggplot(toc_comb, aes(x = q, y = estimate, colour = game)) +\n  ref_zero +\n  geom_ribbon(\n    aes(ymin = lower, ymax = upper, fill = game, group = game),\n    alpha = 0.15,\n    colour = NA\n  ) +\n  geom_line(aes(linetype = game), linewidth = 0.8) +\n  scale_colour_game() +\n  scale_linetype_game() +\n  scale_fill_manual(values = game_cols, name = NULL) +\n  guides(fill = \"none\") +\n  labs(\n    title = sprintf(\n      \"TOC by game: AUTOC = %.3f (Dictator), %.3f (Trust)\",\n      rate_dict$estimate,\n      rate_trust$estimate\n    ),\n    x = \"Treated fraction q (highest predicted CATE first)\",\n    y = \"ATE(top q) - ATE(all)\"\n  ) +\n  theme_mbeu()\n```\n\n::: {.cell-output-display}\n![CF Targeting Operator Characteristic curve, both games overlaid: ATE among the top-q fraction by predicted CATE minus the overall ATE, with 95% CI ribbon. The area under each curve is the AUTOC, reported in the title.](07_postprocess_grf_files/figure-html/fig-grf-combined-toc-1.png){#fig-grf-combined-toc width=3250}\n:::\n:::\n\n\n\n::: {#cell-fig-grf-combined-blp .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-combined-blp\n#| fig-cap: \"CF best linear projection of the CATE onto the moderator set, both games overlaid, with 95% CIs (dodged by game). Terms are ordered by their mean coefficient across the two games. Appendix-style linear summary of effect modification.\"\n#| dpi: 500\n#| fig-width: 6.5\n#| fig-height: 9\nblp_comb <- bind_rows(\n  blp_dict_df |> mutate(game = \"Dictator game\"),\n  blp_trust_df |> mutate(game = \"Trust game\")\n) |>\n  mutate(game = factor(game, levels = c(\"Dictator game\", \"Trust game\")))\nblp_ord <- blp_comb |>\n  summarise(m = mean(estimate), .by = term) |>\n  arrange(m) |>\n  pull(term)\nblp_comb <- blp_comb |> mutate(term = factor(term, levels = blp_ord))\n\nggplot(blp_comb, aes(x = estimate, y = term, colour = game, shape = game)) +\n  geom_vline(xintercept = 0, linetype = \"dashed\", colour = \"grey60\") +\n  geom_pointrange(aes(xmin = lower, xmax = upper), position = dodge) +\n  scale_colour_game() +\n  scale_shape_game() +\n  labs(\n    title = \"Best linear projection of the CATE, both games\",\n    x = \"Projection coefficient\",\n    y = NULL\n  ) +\n  theme_mbeu()\n```\n\n::: {.cell-output-display}\n![CF best linear projection of the CATE onto the moderator set, both games overlaid, with 95% CIs (dodged by game). Terms are ordered by their mean coefficient across the two games. Appendix-style linear summary of effect modification.](07_postprocess_grf_files/figure-html/fig-grf-combined-blp-1.png){#fig-grf-combined-blp width=3250}\n:::\n:::\n\n\n\n::: {#cell-fig-grf-combined-vi .cell}\n\n```{.r .cell-code .hidden}\n#| label: fig-grf-combined-vi\n#| fig-cap: \"CF variable importance aggregated to the original moderators, both games overlaid (dodged bars by game). Profile-level and respondent-level moderators are separated into facets. Within each facet moderators are ordered by their mean importance across the two games. Country fixed-effect columns excluded.\"\n#| dpi: 500\n#| fig-width: 7\n#| fig-height: 9\nvi_comb <- bind_rows(\n  vi_dict_df |> mutate(moderator = as.character(moderator), game = \"Dictator game\"),\n  vi_trust_df |> mutate(moderator = as.character(moderator), game = \"Trust game\")\n) |>\n  mutate(game = factor(game, levels = c(\"Dictator game\", \"Trust game\")))\nvi_ord <- vi_comb |>\n  summarise(m = mean(importance), .by = moderator) |>\n  arrange(m) |>\n  pull(moderator)\nvi_comb <- vi_comb |> mutate(moderator = factor(moderator, levels = vi_ord))\n\nggplot(vi_comb, aes(x = importance, y = moderator, fill = game)) +\n  geom_col(position = position_dodge(width = 0.7), width = 0.6) +\n  facet_wrap(~family, scales = \"free_y\", ncol = 1) +\n  scale_fill_manual(values = game_cols, name = NULL) +\n  labs(\n    title = \"Variable importance, both games\",\n    x = \"Importance (depth-weighted split frequency)\",\n    y = NULL\n  ) +\n  theme_mbeu()\n```\n\n::: {.cell-output-display}\n![CF variable importance aggregated to the original moderators, both games overlaid (dodged bars by game). Profile-level and respondent-level moderators are separated into facets. Within each facet moderators are ordered by their mean importance across the two games. Country fixed-effect columns excluded.](07_postprocess_grf_files/figure-html/fig-grf-combined-vi-1.png){#fig-grf-combined-vi width=3500}\n:::\n:::\n\n\n## Session Info {#sec-session-info .appendix .unnumbered}\n\n\n::: {.cell}\n\n```{.r .cell-code}\n#| label: session-info\n#| eval: true\n#| echo: true\nsession_info()\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n─ Session info ───────────────────────────────────────────────────────────────\n setting  value\n version  R version 4.5.3 (2026-03-11 ucrt)\n os       Windows 11 x64 (build 26200)\n system   x86_64, mingw32\n ui       RTerm\n language (EN)\n collate  English_United States.utf8\n ctype    English_United States.utf8\n tz       Europe/Berlin\n date     2026-06-10\n pandoc   3.8.3 @ c:\\\\Program Files\\\\Positron\\\\resources\\\\app\\\\quarto\\\\bin\\\\tools/ (via rmarkdown)\n quarto   1.9.36 @ C:\\\\PROGRA~1\\\\Quarto\\\\bin\\\\quarto.exe\n\n─ Packages ───────────────────────────────────────────────────────────────────\n package      * version    date (UTC) lib source\n abind          1.4-8      2024-09-12 [1] CRAN (R 4.5.0)\n backports      1.5.0      2024-05-23 [1] CRAN (R 4.5.0)\n broom          1.0.11     2025-12-04 [1] CRAN (R 4.5.2)\n car            3.1-3      2024-09-27 [1] CRAN (R 4.5.1)\n carData        3.0-5      2022-01-06 [1] CRAN (R 4.5.1)\n cli            3.6.5      2025-04-23 [1] CRAN (R 4.5.1)\n codetools      0.2-20     2024-03-31 [2] CRAN (R 4.5.3)\n digest         0.6.39     2025-11-19 [1] CRAN (R 4.5.3)\n dplyr        * 1.1.4      2023-11-17 [1] CRAN (R 4.5.1)\n evaluate       1.0.5      2025-08-27 [1] CRAN (R 4.5.2)\n farver         2.1.2      2024-05-13 [1] CRAN (R 4.5.1)\n fastmap        1.2.0      2024-05-15 [1] CRAN (R 4.5.1)\n forcats      * 1.0.1      2025-09-25 [1] CRAN (R 4.5.2)\n Formula        1.2-5      2023-02-24 [1] CRAN (R 4.5.0)\n generics       0.1.4      2025-05-09 [1] CRAN (R 4.5.1)\n ggplot2      * 4.0.1      2025-11-14 [1] CRAN (R 4.5.2)\n ggpubr       * 0.6.2      2025-10-17 [1] CRAN (R 4.5.2)\n ggsignif       0.6.4      2022-10-13 [1] CRAN (R 4.5.1)\n glue           1.8.1      2026-04-17 [1] CRAN (R 4.5.3)\n grf          * 2.6.1      2026-03-04 [1] CRAN (R 4.5.3)\n gtable         0.3.6      2024-10-25 [1] CRAN (R 4.5.1)\n here         * 1.0.2      2025-09-15 [1] CRAN (R 4.5.2)\n hms            1.1.4      2025-10-17 [1] CRAN (R 4.5.2)\n htmltools      0.5.9      2025-12-04 [1] CRAN (R 4.5.2)\n htmlwidgets    1.6.4      2023-12-06 [1] CRAN (R 4.5.1)\n jsonlite       2.0.0      2025-03-27 [1] CRAN (R 4.5.3)\n knitr          1.51       2025-12-20 [1] CRAN (R 4.5.2)\n labeling       0.4.3      2023-08-29 [1] CRAN (R 4.5.0)\n lattice        0.22-9     2026-02-09 [2] CRAN (R 4.5.3)\n lifecycle      1.0.5      2026-01-08 [1] CRAN (R 4.5.2)\n lmtest         0.9-40     2022-03-21 [1] CRAN (R 4.5.1)\n lubridate    * 1.9.5      2026-02-04 [1] CRAN (R 4.5.3)\n magrittr       2.0.4      2025-09-12 [1] CRAN (R 4.5.2)\n Matrix         1.7-4      2025-08-28 [2] CRAN (R 4.5.3)\n otel           0.2.0      2025-08-29 [1] CRAN (R 4.5.1)\n patchwork    * 1.3.2      2025-08-25 [1] CRAN (R 4.5.2)\n pillar         1.11.1     2025-09-17 [1] CRAN (R 4.5.2)\n pkgconfig      2.0.3      2019-09-22 [1] CRAN (R 4.5.1)\n purrr        * 1.2.1      2026-01-09 [1] CRAN (R 4.5.2)\n R6             2.6.1      2025-02-15 [1] CRAN (R 4.5.1)\n RColorBrewer   1.1-3      2022-04-03 [1] CRAN (R 4.5.0)\n Rcpp           1.1.1      2026-01-10 [1] CRAN (R 4.5.2)\n readr        * 2.1.6      2025-11-14 [1] CRAN (R 4.5.2)\n rlang          1.1.7      2026-01-09 [1] CRAN (R 4.5.2)\n rmarkdown      2.30       2025-09-28 [1] CRAN (R 4.5.2)\n rprojroot      2.1.1      2025-08-26 [1] CRAN (R 4.5.2)\n rstatix        0.7.3      2025-10-18 [1] CRAN (R 4.5.2)\n S7             0.2.1      2025-11-14 [1] CRAN (R 4.5.2)\n sandwich       3.1-1      2024-09-15 [1] CRAN (R 4.5.1)\n scales         1.4.0      2025-04-24 [1] CRAN (R 4.5.1)\n sessioninfo  * 1.2.3.9000 2025-09-23 [1] Github (r-lib/sessioninfo@ec4dd0c)\n stringi        1.8.7      2025-03-27 [1] CRAN (R 4.5.0)\n stringr      * 1.6.0      2025-11-04 [1] CRAN (R 4.5.2)\n tibble       * 3.3.1      2026-01-11 [1] CRAN (R 4.5.2)\n tidyr        * 1.3.2      2025-12-19 [1] CRAN (R 4.5.2)\n tidyselect     1.2.1      2024-03-11 [1] CRAN (R 4.5.1)\n tidyverse    * 2.0.0      2023-02-22 [1] CRAN (R 4.5.3)\n timechange     0.4.0      2026-01-29 [1] CRAN (R 4.5.3)\n tzdb           0.5.0      2025-03-15 [1] CRAN (R 4.5.1)\n vctrs          0.6.5      2023-12-01 [1] CRAN (R 4.5.1)\n withr          3.0.2      2024-10-28 [1] CRAN (R 4.5.1)\n xfun           0.55       2025-12-16 [1] CRAN (R 4.5.2)\n yaml           2.3.12     2025-12-10 [1] CRAN (R 4.5.3)\n zoo            1.8-15     2025-12-15 [1] CRAN (R 4.5.2)\n\n [1] C:/Users/Tris/AppData/Local/R/win-library/4.5\n [2] C:/Program Files/R/R-4.5.3/library\n * ── Packages attached to the search path.\n\n──────────────────────────────────────────────────────────────────────────────\n```\n\n\n:::\n:::\n\n\n## Execution Time {#sec-exec-time .appendix .unnumbered}\n\n\n::: {.cell}\n\n```{.r .cell-code}\n#| label: exec-time\n#| eval: true\n#| echo: true\n#| include: true\nend_time <- Sys.time()\nexec_time <- end_time - start_time\ncat(paste(\n  \"R code execution time:\",\n  round(as.numeric(exec_time, units = \"secs\"), 2),\n  \"seconds.\"\n))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nR code execution time: 389.62 seconds.\n```\n\n\n:::\n:::\n\n",
    "supporting": [],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {},
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}